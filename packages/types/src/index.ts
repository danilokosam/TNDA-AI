/**
 * Scaffolded, not yet populated. The backend's own schemas are Zod v3, the
 * frontend's (frontend/types/api.ts) are Zod v4 — a deliberate, already-made
 * choice, not an oversight — and backend code assumes Bun-only APIs in
 * places. Moving either into here isn't a mechanical relocation; it's a
 * deliberate future decision about what should actually be shared. See the
 * monorepo migration entry in the root PROGRESS.md for the full reasoning.
 */
export {};
