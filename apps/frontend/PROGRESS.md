# TNDA-AI Frontend — Progress

## 1. Architecture & Planning (2026-08-06)

Before any code was written, the backend's actual API surface was re-verified directly from its route/schema/service source (not from memory), and current library versions were checked against the npm registry rather than assumed. This surfaced six real gaps between the spec and what the backend actually provides — no list/history endpoint, no refresh endpoint, no logout endpoint, original files never persisted, no `document_type`/confidence columns on `document_jobs`, and (the one gap that's actually an opportunity) `document_jobs` RLS already scopes `select` to the caller's own org, usable directly from the frontend.

A second-opinion review (independent agent pass reading the real backend source) then stress-tested the proposed auth architecture specifically and found several concrete implementation problems worth correcting before writing code: the backend's IP-keyed rate limiter becomes a bucket shared across *all* users once every call is proxied through one Next.js server (relevant because job-status polling is the app's highest-volume traffic pattern by design); Server Components can't write cookies, so refresh has to be centralized in `proxy.ts`, not scattered per-caller; naive multipart proxying via `request.formData()` would fully buffer uploads in memory; and logout needs to actually revoke the refresh token server-side, not just clear cookies, to be consistent with the reason httpOnly cookies were chosen in the first place.

A subsequent self-review pass (done deliberately before implementation, hunting for unnecessary complexity rather than confirming the first draft) cut a generic catch-all `/api/backend/[...path]` proxy in favor of small purpose-built Route Handlers (the "generic" proxy was never actually generic — upload needs stream-passthrough handling and billing needs URL injection, so it would've special-cased its own traffic anyway), dropped a since-unnecessary `/api/auth/refresh` route, found that Dashboard needs an aggregate stats query that's genuinely separate from the paginated History endpoint (an aggregate can't be correctly derived from one page of a paginated list), and switched History pagination from offset to keyset/cursor-based (offset pagination drifts under concurrent inserts — a real case here, since a user can upload a new batch while paging through their own history).

**Decisions made, with reasoning kept here rather than scattered across commit messages:**

- **Document preview (Results page) is session-only for v1.** The backend intentionally never persists uploaded files, so a *persistent* preview isn't possible without new infrastructure. The just-uploaded file is cached client-side and shown via a blob URL through the immediate upload→processing→results flow; opening an older job from History shows an explicit "preview unavailable" state instead of a broken image. Built behind a `DocumentPreviewSource` abstraction (`session-blob` / `remote-url` / `unavailable`) specifically so a persistent backend-provided URL can replace the session-only source later without touching `DocumentPreviewPanel` or any other UI — this was an explicit requirement, not an implementation nicety.
- **Auth session lifecycle:** the backend's `/auth/signup`/`/auth/login` still do all real orchestration (organization + profile creation, with rollback on partial failure) — calling `@supabase/ssr`'s own signup/login directly would bypass that and leave orphaned `auth.users` rows with no `profiles` row. Instead, immediately after the backend responds, `@supabase/ssr`'s `setSession()` hydrates a server-side Supabase client, which then owns the cookie/refresh lifecycle from that point on (`getUser()`-based refresh centralized in `proxy.ts`, on every request, not per-caller). Logout calls `supabase.auth.signOut()` server-side to actually revoke the refresh token, then clears cookies.
- **Type safety across the API boundary uses Elysia Eden, not hand-written DTOs or an OpenAPI codegen step.** `backend/src/index.ts` already ends with `export type App = typeof app` — Elysia's own convention for exactly this. `types/api.ts` re-exports types derived from a type-only `import type { App } from ".../backend/src/index"`, erased entirely at compile time (nothing backend-side is bundled or executed differently). A backend field rename becomes an immediate frontend type error instead of a silent runtime mismatch.
- **Rate limiting is a real, backend-side ceiling this frontend can manage but not fix** (100 req/60s, keyed by IP — shared across all users once proxied, since `rate-limit.middleware.ts` sees the Next.js server's IP, not each end user's). Job-status polling uses a backoff schedule (~2s/3s/5s/8s, capped ~10s, stopping at any terminal status) rather than a fixed fast interval — necessary, not just gentler UX. The real fix (keying the backend's limiter on user ID instead of IP) is out of scope since it requires a backend change.
- **Call paths are fixed and enforced, not just documented.** Server Components import `services/*.service.ts` directly; Client Components only reach it indirectly through this app's own `/api/*` Route Handlers. Every file under `services/` starts with `import "server-only"` so an accidental Client Component import fails the build instead of silently risking a path to backend tokens in a client bundle.
- **CSRF is covered by `SameSite=Lax` + all-mutations-are-POST, deliberately, not a token.** `SameSite=Lax` was already required for a different reason (Stripe Checkout/Portal redirect back to the app via top-level GET — `Strict` would silently log the user out right after they pay), and combined with every mutating route here being POST, that already closes the practical CSRF gap without adding a separate token mechanism.
- **Optimistic updates were checked against every real mutation in the app and don't fit v1 anywhere** (upload-queue entries are already transient client-only state; "mark as reviewed" is explicitly local-only since there's no backend endpoint to persist it to; a plan change redirects to Stripe and can't be optimistically shown before payment succeeds; History has no mutations). Recorded so this reads as a considered decision, not a skipped requirement.

## 2. Stage 0: Bootstrap (2026-08-06)

- `create-next-app` (Next 16.3.0, React 19.2.8, TypeScript 5.9.3, Tailwind v4.3.3, App Router, Turbopack, no `src/` directory, Bun as package manager) scaffolded directly into `frontend/`. TypeScript pinned to the 5.x line rather than the `latest` dist-tag (7.0.2) — TS7 is a Go-rewrite RC whose programmatic API (needed by `typescript-eslint`) isn't stable until 7.1; the backend pins to a 5.x range for the same reason.
- `tsconfig.json` tightened to match the backend's strictness rather than Next's more lenient default: added `noUncheckedIndexedAccess`, `noImplicitOverride`, `noFallthroughCasesInSwitch`, `noUnusedLocals`, `noUnusedParameters`, `forceConsistentCasingInFileNames`, and set `allowJs: false`. `@/*` maps to the project root (no `src/` dir, per the planned top-level `app/ components/ features/ ...` layout).
- `shadcn init` (Radix primitives base, Nova/Lucide preset, CSS variables, pointer cursor on buttons) — added `components/ui/button.tsx`, `lib/utils.ts`, `components.json`, and a full OKLCH-based design-token system in `app/globals.css` (light + dark, including the `--chart-1..5` tokens that later become the charting palette — this is why the plan chose shadcn's own chart primitives over a separate library like Tremor: one token system instead of two).
- Remaining stack installed at the exact versions verified during planning: `@tanstack/react-query@5.101.4`, `@tanstack/react-table@9.0.0`, `react-hook-form@7.84.0`, `zod@4.4.3`, `@hookform/resolvers@5.7.1`, `motion@13.0.0`, `@supabase/supabase-js@2.112.2`, `@supabase/ssr@0.12.4`, `@elysiajs/eden@1.4.9`, `server-only@0.0.1`.
- `package.json` scripts: `dev`/`start` pinned to port **3001** (backend defaults to 3000 — this avoids collision without touching any backend config), plus `typecheck` added to match the backend's script naming. The `shadcn` CLI package was moved from `dependencies` to `devDependencies` (it's a build-time tool, never imported by application code).
- `.gitignore`: added `!.env.example` (the generated default ignored `.env.example` along with everything else matching `.env*`, which would have kept the template out of version control — same fix the backend's own `.gitignore` already has).
- `.env.example` added (`BACKEND_URL`, `SUPABASE_URL`, `SUPABASE_ANON_KEY` — all server-only by convention, nothing `NEXT_PUBLIC_`-prefixed). Local `.env` populated with the same Supabase project the backend uses, for real end-to-end verification during development.
- Removed `AGENTS.md`/`CLAUDE.md` (auto-generated by `create-next-app`, not requested).
- Verified: `bun run typecheck` clean, `bun run dev` boots and serves on `:3001`.

## 3. Stage 1: App shell + auth (2026-08-06)

### Architecture change: the planned Eden Treaty type-sourcing doesn't work — discovered by testing, not assumed

The plan's "Type safety across the boundary" section called for a type-only `import type { App } from "backend/src/index"` (Elysia's Eden Treaty pattern) so `types/api.ts` would need zero hand-written DTOs. Tested directly before writing any real code, and it fails: `backend/src/index.ts` transitively imports every route module via the backend's own `@/*` alias (`@/config/env`, `@/modules/auth/auth.routes`, etc.). TypeScript resolves those imports using the **importing project's** `tsconfig.json` `paths`, not the imported file's own — so once pulled into a frontend file, `@/config/env` resolves against `frontend/@/*` (`./*`) instead of `backend/@/*` (`src/*`), and fails outright. This isn't fixable without either editing the backend's `tsconfig.json` (not allowed) or a formal TS project-references/monorepo restructuring (out of scope, and would also touch the backend).

The fallback considered next — `openapi-typescript` codegen against the backend's own mounted `@elysiajs/swagger` (`/docs/json`) — was also tested against the live endpoint and doesn't work either: `@elysiajs/swagger` is serializing raw Zod-internal `_def` trees (`{"_def":{"unknownKeys":"strip","typeName":"ZodObject",...}}`) instead of real JSON Schema for every request body, and every response schema comes back as a bare `{}` — confirmed by fetching and inspecting `http://localhost:3000/docs/json` directly, not just the request bodies but response bodies too (`GET /documents/jobs/{id}` → `"responses":{"200":{}}`). `openapi-typescript` would generate either build errors or meaningless types from this.

**Landed on a hybrid that's arguably more robust than either original option:**
- Files with zero internal imports — `backend/src/config/database.types.ts` (the `Database` type) and `backend/src/utils/errors.ts` (`AppErrorCode`) — **do** resolve cleanly as type-only cross-project imports (verified directly, including a deliberate-break test to confirm the check isn't a false pass — see below). These are type-only imported for real, with zero hand-duplication.
- Everything else (request bodies, and response shapes that are service-layer inventions rather than raw DB rows — `AuthResponse`, `UsageSummary`, `SubscriptionResponse`, `JobDto`, `BatchResult`, etc.) is hand-written as Zod schemas in `types/api.ts`, verified field-by-field against the real route/service/schema source (re-read directly, not from memory — `auth.schema.ts`, `auth.service.ts`, `organization.service.ts`/`.repository.ts`, `billing.schema.ts`/`.service.ts`, `documents.schema.ts`/`.service.ts`, `database.types.ts`).
- Where a hand-written schema mirrors a type-importable DB row (`organizations`, `plans`, `subscriptions`), an `AssertExact<A, B>` compile-time check (`export const _checkX: AssertExact<z.infer<typeof xSchema>, XRow> = true`) makes a future drift a build error, not just a hope. **This actually didn't work on the first attempt** — the first version used `declare const _checkX: AssertExact<...>` (an ambient declaration), which type-checks successfully regardless of what `AssertExact` resolves to, since declaring a variable of type `never` isn't itself an error. Caught this by deliberately breaking a field (`price_monthly: z.number()` → `z.string()`) and confirming `tsc` still passed — it did, silently, which meant the "safety net" was inert. Fixed by switching to a real assignment (`export const _checkX: AssertExact<...> = true`), which forces TypeScript to actually validate `true` against the computed type; re-ran the same deliberate-break test and confirmed it now fails the build as intended, then restored the correct version.
- `lib/api/backend-client.ts` (server-only) and `lib/api/http.ts` (client-safe, calls this app's own `/api/*` routes) both parse every response through the matching Zod schema via a shared `parseApiResponse` helper — so a real backend/schema drift is caught at runtime too, at the exact call site, not just hoped not to happen. This is arguably a stronger guarantee than Eden would have given (compile-time only, no runtime check).
- `@elysiajs/eden` was removed from `package.json` (installed in Stage 0 for the approach that didn't pan out).

### Auth implementation

- **Session lifecycle**: `/api/auth/{signup,login}` call `services/auth.service.ts` (thin wrapper over `backendFetch`, no validation of its own — validation happens once, at the Route Handler, via `parseJsonBody` against the matching Zod schema, mirroring the backend's own routes-validate/services-trust layering), then call `@supabase/ssr`'s `setSession()` to hydrate cookies from the returned tokens. `/api/auth/logout` calls `signOut()` (real server-side revocation, not just cookie clearing) — idempotent by design.
- **`proxy.ts`**: one `getUser()` call per request (transparent refresh + re-cookie), then three outcomes depending on path — `/` redirects based on auth state; `/api/*` paths 401 (JSON, not a redirect — these are `fetch()` callers) if unauthenticated, except `/api/auth/*` itself; every other page redirects to `/login` if unauthenticated, or `/login`↔`/signup` redirect to `/dashboard` if already authenticated (the reciprocal redirect). Matcher covers everything except `_next/static`, `_next/image`, and `favicon.ico` — deliberately includes `/api/*`, or polling requests later would bypass the refresh guarantee entirely.
- **Call-path rule enforced, not just documented**: every `services/*.service.ts` file starts with `import "server-only"`. Server Components (`(dashboard)/layout.tsx`) call services directly; the one Client-Component-reachable path so far (`features/auth/hooks.ts`) goes through `lib/api/http.ts` → this app's own `/api/*` routes → the same service functions.
- **Dashboard shell**: `(dashboard)/layout.tsx` (Server Component) fetches the current user once and passes it down as a prop to `AppSidebar`/`UserMenu` — deliberately **not** a `useCurrentUser()` query hook + `HydrationBoundary` yet, since nothing client-side independently needs to refetch this data in Stage 1. That pattern is worth introducing once something actually does (e.g. Stage 2's usage numbers, refetched after an upload) — building it now for a duplicate-fetch problem that doesn't exist yet would be exactly the kind of premature abstraction the spec warns against. Sidebar built on shadcn's own `sidebar.tsx` (collapsible, icon mode, mobile sheet) rather than hand-rolled. shadcn's `form.tsx` registry item turned out to be an empty stub in the installed CLI/registry version (`shadcn view form` returns a bare `{"name":"form"}` with no file content) — login/signup forms use React Hook Form's `register()`/`formState.errors` directly against the `Input`/`Label` primitives instead of the `FormField` wrapper; a contained, easy swap later if the registry gets real content.
- Fixed two real lint findings before considering this done: (1) `features/auth/hooks.ts`'s logout originally used a hard `window.location.assign()` reload specifically to guarantee TanStack Query's cache doesn't leak a just-logged-out user's data into a next login in the same tab — Next's linter flagged this; switched to `queryClient.clear()` + `router.push()`, which achieves the same guarantee through the correct tool instead of a hard reload. (2) shadcn's own generated `hooks/use-mobile.ts` fails `react-hooks/set-state-in-effect` — the flagged line is a deliberate SSR-hydration-safety pattern (server and the client's first render must both start from the same `undefined` value, corrected only after mount), not an oversight, so switching to a `useState` lazy initializer (the "fix" the rule implies) would have reintroduced a real hydration-mismatch risk; kept the original pattern with a scoped, documented `eslint-disable-next-line`.

### Verification

Both `bun run typecheck` and `bun run lint` clean. End-to-end against the real backend and real Supabase project (both running locally), via `curl` with a real cookie jar — no browser tool available in this environment, so this is as close to real verification as achievable here: signup (real org+profile created, `Stage1 Test Org`/`owner`, single non-chunked `SameSite=lax` cookie set) → `/dashboard` renders 200 with the real org name present in the HTML → `/login` while authenticated redirects to `/dashboard` → `/api/organization` returns real usage data (`free` plan, `2` pages/`5` docs remaining) parsed successfully through the hand-written Zod schemas, which is itself a real-world confirmation those schemas are accurate, not just carefully read → logout returns `{success:true}` and actually clears the cookie (not left stale) → `/dashboard` and `/api/organization` both correctly deny access again afterward → login with the same credentials works → login with a wrong password returns a clean `401 UNAUTHORIZED` (not a crash) → signup with an invalid body returns `400 VALIDATION_ERROR` with real per-field issues → all five stub dashboard pages (`/dashboard /upload /documents /billing /settings`) render `200` → an unknown path returns a real `404`. Dev server log clean throughout — no hydration warnings, no unhandled errors. Not tested (impractical without waiting out a real ~1hr Supabase JWT expiry): the actual token-refresh path in `proxy.ts`; the architecture's correctness there rests on `@supabase/ssr`'s own documented behavior, not an independent empirical check.

## 4. Stage 2: Dashboard (2026-08-07)

Picked up the plumbing the project-level `PROGRESS.md`'s own "Next Session" section had already scoped: extended `types/api.ts` (`documentType`/`averageConfidence` on the job schema; new `organizationStatsSchema`, `documentsListResponseSchema`, `ListDocumentsParams`), added `services/documents.service.ts` (`listDocuments`) and `services/billing.service.ts` (`getSubscription` only — full `plans`/`checkout`/`portal` deferred to Stage 6), extended `services/organization.service.ts` with `getJobStats`, and added three thin BFF routes (`app/api/organization/stats`, `app/api/documents` GET-only, `app/api/billing/subscription`) matching the existing `app/api/organization/route.ts` pattern exactly.

Then the Dashboard page itself (`app/(dashboard)/dashboard/page.tsx`) — an async Server Component, not a client-side query, same precedent as `(dashboard)/layout.tsx`'s own user fetch: nothing here needs client-side refetching yet, so it calls `services/*` directly rather than its own BFF routes (which exist anyway, for Stage 5/6 and any future client interaction on this page). Fetches `getOrganizationOverview`, `getJobStats`, `getSubscription` in parallel via `Promise.all`, then renders either the empty state or:
- Three `StatTile`s (documents processed, success rate, avg. processing time) — `null` stats render as `"—"` with a "No completed jobs yet" subtext, never a fabricated `0` (mirrors the backend's own `null`-not-`0` convention through to the UI).
- `DocumentsTrendChart` — a hand-rolled inline SVG line+soft-area chart (not a charting library; see below), with a pointer- and keyboard-operable crosshair/tooltip and a table-view toggle.
- `CurrentPlanCard` — plan name/price, a subscription-status `Badge`, and two `UsageMeter`s (documents/pages used vs. plan limits).

**Color**: this theme (`shadcn init`'s neutral base, Stage 0 above) had never used a real hue — `--chart-1..5` were unpopulated grays. Used the `dataviz` skill's validated default categorical slot 1 (blue) for the single-series chart, but didn't take the skill's example surfaces on faith: wrote a small throwaway OKLCH→sRGB conversion script to get this theme's *actual* computed card-surface hex (`#ffffff` light, `#171717` dark, from its real `oklch()` tokens), then ran `scripts/validate_palette.js` against those real surfaces. `--chart-1` updated in `app/globals.css` for both modes; `chart-2..5` untouched.

**Chart implementation**: hand-rolled SVG, not shadcn's `chart.tsx`/Recharts. Worth flagging plainly against this file's own Stage 0 section above, which said the plan anticipated shadcn's own chart primitives specifically for a shared token system: checked the original plan file directly and it never mentions charts at all, so that was this project's own Stage-0-session inference, not a real requirement from planning. The token-system goal is still met (the chart reads `var(--color-chart-1)`, the same variable the rest of the theme uses) — but the rendering approach is a genuinely different call, made without having re-read that note first. No dependency was added; a single-series line chart with the `dataviz` skill's specific interaction requirements (arrow-key-operable crosshair, a table-view twin) didn't need one. Revisit if a later stage's charting needs (multi-series, more chart types) outgrow what's reasonable to hand-roll.

**Two real fixes from a deliberate second review pass**, done before committing rather than treating the first implementation pass as final: `UsageMeter` had used `role="progressbar"` (semantically wrong — a meter, not an in-progress operation) with an unclamped `aria-valuenow` (could exceed `aria-valuemax` if usage ever exceeds the plan limit); switched to `role="meter"` with a clamped value and an explicit `aria-valuetext`. The Dashboard's empty-state gate checked only `stats.totalJobs` (terminal jobs) — verified directly against `0010_document_analytics.sql`'s actual SQL that `dailyCounts` uses the identical status filter, so that part was already consistent — but an org with a job still `pending`/`processing` would incorrectly see "no documents yet"; added the already-fetched `usage.documentsUsed` (which does count pending/processing) to the gate.

### Verification

Full workspace `typecheck`/`lint`/`test` clean (`pnpm typecheck`/`lint`/`test` from the repo root), including this project's strict `noUncheckedIndexedAccess` — the chart component in particular needed real guards (early-return checks, not `!` assertions) for every dynamic array index, since establishing `n > 0` via an early return doesn't get automatically correlated to the safety of `dailyCounts[someExpression]` by the type checker.

No browser tool available this session (same as Stage 1) — fell back to the same `curl`-based method: signed up a fresh org against the real backend + Supabase project, confirmed the Dashboard's empty-state path from the actual server-rendered HTML (not just a 200 status — grepped for the real copy and the upload CTA's `href`), and called `/api/organization`, `/api/organization/stats`, `/api/billing/subscription`, `/api/documents` directly through their real BFF routes with that session's cookie — all four returned 200 with real data that parsed cleanly through the exact Zod schemas the Dashboard renders. Re-ran the empty-state check a second time after the review pass's gate-condition fix, against the same org, to confirm no regression.

**Not verified**: the populated view's actual rendering (stat tiles/chart/plan card against real non-zero data) — needs a real completed document job, which needs either a real Azure Document Intelligence call or a database seed. Asked the user; they chose to defer this rather than have real Azure credits spent unprompted. They're now running both apps locally themselves to check this directly before Stage 3 starts.

## 5. Test infrastructure + Stage 3: Upload + Processing (2026-08-08)

### Test infrastructure

Asked to follow an incremental TDD workflow for Stage 3 (types/contracts → failing tests → minimal implementation → full-workspace verify, per feature category: services, hooks, state management, components) — the frontend had no test runner at all before this. Added `vitest`, `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom`, `jsdom` (current registry versions checked, not assumed). `vitest.config.mts` (`.mts`, not `.ts` — avoids the same CommonJS/ESM config-loader warning the CI-typegen fix session hit and fixed by extension, not by touching `package.json`'s `"type"` field) mirrors the backend's own `vitest.config.ts`: `resolve.alias` for `@`, dummy `SUPABASE_URL`/`SUPABASE_ANON_KEY`/`BACKEND_URL` so `lib/env.ts`'s eager Zod validation doesn't need real credentials.

Two environment problems, both found and fixed before any real feature test was written:
- `server-only`'s default export throws unconditionally outside Next's own bundler (Next resolves it to a no-op via the `react-server` exports condition; Vitest doesn't set that condition). Aliased `server-only` → `test/mocks/server-only.ts` (a one-line no-op) rather than setting `react-server` globally in the Vitest config, which risked changing how React itself resolves for component tests.
- Testing-Library's auto-cleanup between tests self-registers via a global `afterEach` — this project imports `describe`/`it`/`expect` from `"vitest"` per file rather than turning on `test.globals` (matching the backend's style), so that detection silently never fires. Symptom: "multiple elements found" once a test file has more than one test that renders the same element. Found on `DocumentTypeSelect`'s first test (§ below); fixed once in `test/setup.ts` with an explicit `afterEach(() => cleanup())`.

`test/query-client-wrapper.tsx` (`createQueryWrapper()`) is the shared `renderHook`/`render` wrapper for anything touching React Query — a fresh `QueryClient` per call, `retry: false` on both queries and mutations so a mocked rejection doesn't sit through retry delays in tests.

### Services: `documents.service.ts#uploadDocument`/`getJobStatus`

`UploadDocumentParams` (new, in `types/api.ts`) pairs with the `uploadResponseSchema` that already existed from Stage 1. `uploadDocument` builds a `FormData` (`file`, and `documentType` only when provided — omitted rather than defaulted client-side, matching the backend's own default of `"invoice"`) and posts it through `backendFetch`; `getJobStatus` GETs `/api/v1/documents/jobs/:id` with the id URL-encoded. Tests mock `backendFetch` (one layer below what's under test, matching the backend's own repository-mocking style) and assert on the actual `FormData`/path/schema passed, not just that *a* call happened.

BFF routes: `POST` added to the existing `app/api/documents/route.ts` (reads `request.formData()`, 400 `VALIDATION_ERROR` locally — without ever calling `uploadDocument` — if `file` isn't a real `File` instance), and a new `app/api/documents/[jobId]/route.ts` (`GET`, async `params` per Next 16's route-handler convention — `{ params }: { params: Promise<{ jobId: string }> }`).

Writing the POST route's own test hit a real bug: `new Request(url, { method: "POST", body: formData })` followed by `request.formData()` inside the handler threw `AssertionError [ERR_ASSERTION]` deep in `node:internal/deps/undici/undici`'s `multipartFormDataParser`, specifically `webidl.is.File(value)` failing. Wrote a 5-line repro in plain Node (no Vitest) — identical code, worked fine — which pinned the cause on jsdom: jsdom ships its own `File`/`FormData` (they're DOM interfaces, not just Fetch API ones), and a jsdom-constructed `File` fails undici's internal brand check when it reaches the *real* `Request.formData()` parser Node provides. Fixed with `// @vitest-environment node` at the top of both new route test files — Route Handlers are server-side Node code regardless, so this is the more correct environment choice on its own merits, not just a workaround.

### Hooks: `useUploadDocument`/`useJobStatus`, and the backoff schedule

`features/upload/hooks.ts`. `useUploadDocument` is a plain `useMutation` wrapping `apiFetch` the same way `uploadDocument` wraps `backendFetch`. `useJobStatus(jobId: string | null)` is a `useQuery` with a dynamic `refetchInterval`; passing `null` (rather than requiring the caller to conditionally call the hook) disables it via `enabled: jobId !== null` — needed because a queue of upload items, some without a job yet, all need to call this hook unconditionally, one per item (see components, below).

The backoff schedule (2s/3s/5s/8s, capped at 10s, stopping once the job is `completed`/`failed`/`rejected_quota`) is `getNextPollInterval(status, fetchCount)` in `features/upload/poll-schedule.ts` — a pure function, deliberately pulled out of the `refetchInterval` callback itself. `fetchCount` is React Query's own `query.state.dataUpdateCount`. 12 plain unit tests (no timers, no rendering) cover every step, the cap, and all three terminal statuses.

Testing that this pure function is actually *wired in* correctly needed real fake timers, which surfaced a second real bug: `@testing-library/react`'s `waitFor` polls via a genuine `setInterval` and simply never resolves once `vi.useFakeTimers()` has frozen time — a test hung for the full default 5000ms timeout before failing with a timeout error, not a useful assertion failure, which is what made it non-obvious at first. Diagnosed by bisecting with `vi.advanceTimersByTimeAsync` steps directly instead of `waitFor`. Fixed with a small local `advanceUntil(predicate, maxMs, stepMs)` helper (advances in `stepMs` increments, flushing microtasks after each, until `predicate()` is true or `maxMs` is exhausted) — used only inside the fake-timer describe block. The test asserts *ranges* ("not within 500ms," "eventually, within a few seconds") rather than the exact scheduled millisecond, on purpose: matching React Query's precise internal tick count isn't part of this hook's actual contract.

### State management: `features/upload/queue.ts`

`UploadQueueItemStatus = "queued" | "uploading" | "upload-failed" | JobDto["status"] | "batch-submitted"` — reuses the backend's own status union directly (including keeping `rejected_quota` distinct from `failed`, matching how the rest of the app already treats them differently) rather than a parallel enum, plus the states that exist before a job does. `uploadQueueReducer` (7 action types: `add`/`remove`/`upload-started`/`upload-succeeded`/`upload-failed`/`job-status-updated`/`clear-finished`) and `createUploadQueueItem` are both pure — `createUploadQueueItem` takes its `id` as a parameter rather than calling `crypto.randomUUID()` internally, specifically so the reducer module has zero side effects and its tests need no randomness mocked. `useUploadQueue()` is a thin `useReducer` wrapper (`addFiles`/`removeItem`/`clearFinished`/raw `dispatch`) — it has no import of `useUploadDocument`/`useJobStatus` at all; composing state with the network-facing hooks happens one layer up, in the controller (next section). 19 tests across the pure reducer and the hook wrapper.

### The composition point: `features/upload/use-upload-controller.ts#useUploadController`

Combines `useUploadQueue()` + `useUploadDocument()` in a `useEffect`: finds the first `"queued"` item, dispatches `upload-started`, calls the mutation, dispatches `upload-succeeded`/`upload-failed` from its callbacks. An `isUploadingRef` (not state — a ref specifically so setting it doesn't itself trigger a re-render/re-run) guards against starting a second upload while one is in flight, which is what makes uploads process **sequentially**: after one dispatch changes `queue.items`, the effect re-runs, sees the ref still `true`, and bails, until `onSettled` flips it back after the mutation's callbacks have already dispatched the result.

The effect's dependency array is `[queue.items, queue.dispatch, uploadMutation.mutate]`, deliberately *not* `[queue, uploadMutation]` — both of those are fresh object references every render (`useUploadQueue` returns a new object literal each call; `useMutation`'s return value isn't referentially stable either), so depending on the whole objects would re-run the effect on every render regardless of whether the queue's contents actually changed, defeating the ref guard's purpose. This is a real `react-hooks/exhaustive-deps` warning, not a hypothetical one — confirmed by temporarily removing the `eslint-disable-next-line` and re-running lint before deciding to keep it, with a comment explaining why rather than a bare suppression.

Verified sequential (not concurrent) behavior with a real test, not just by reading the code: held the first mocked upload's promise open with an explicit `resolve` captured outside the mock, queued two files, confirmed the second stayed `"queued"` (not `"uploading"`) and `apiFetch` had only been called once — then resolved the first and confirmed the second started. A separate test confirms a rejected upload (`ApiError`) still lets the queue move on to the next file rather than getting stuck.

### Components

`components/upload/`:
- `DocumentTypeSelect` — a native `<select>`, not shadcn's/Radix's Select primitive (no `components/ui/select.tsx` existed, and a plain 4-option select didn't need the added surface area of adding one).
- `Dropzone` — a `<label>` wrapping a visually-hidden `<input type="file">` (free label/input accessibility, no custom ARIA needed), with `onDrop`/`onDragOver`/`onDragLeave` on the label itself. Tested with a real `fireEvent.drop(el, { dataTransfer: { files } })`, not a stub.
- `UploadQueueItemRow` — purely presentational (`item`, `onRemove` props only), a `Record<UploadQueueItemStatus, {label, icon, spin?, destructive?}>` covering all 9 states. Colors follow the same discipline as Stage 2's `UsageMeter` (§4): no invented "success green," just neutral text plus `--destructive` for the actual failure states, since this theme still only has those two non-neutral text treatments.
- `PolledUploadQueueItem` — one `useJobStatus` call per rendered instance (the standard React answer to "a hook per item in a dynamic list," since hooks can't run in a `.map()` callback directly); only polls when `item.jobId !== null && !isFinished(item.status)`, reports fresh data up via an `onStatusUpdate` callback in a `useEffect`, renders `UploadQueueItemRow` underneath.
- `UploadQueueList` — maps items to `PolledUploadQueueItem`, `null` when empty.
- `UploadWorkspace` — the client-side assembly: a document-type selector (applies to whatever gets dropped *next*, not per already-queued item — a `.zip` can only carry one type anyway, and per-row type editing for the non-batch case wasn't asked for), the dropzone, the queue list, and a "Clear finished" button shown only when there's something finished to clear.
- `app/(dashboard)/upload/page.tsx` — now a trivial Server Component (`metadata` + renders `UploadWorkspace`), same split as `DashboardLayout`/`AppSidebar`: `metadata` exports aren't allowed in `"use client"` files, so the interactive tree has to be a child, not the page itself.

Caught one real mistake myself before it was ever run: `UploadQueueItemRow`'s first draft included a redundant local `cn2(...)` class-joining helper instead of importing the shared `cn` from `@/lib/utils` — the same utility `Dropzone.tsx` (written minutes earlier, same session) already used normally. No test would have caught this (it's a style/duplication issue, not a behavioral one) — caught on re-reading the file before running it.

### Verification

Full workspace `typecheck`/`lint`/`test` clean after every one of the four slices, not just once at the end — 90 new tests, 184 total with the backend's 94. No browser tool available this session (same as Stages 1-2) — signed up a fresh org via `curl`, fetched `/upload`, and grepped the real server-rendered HTML for the dropzone copy, the document-type selector, and the page header, confirming the page composes and renders without crashing server-side.

**Not verified**: the actual interactive flow — dragging a real file, watching it move through upload → processing → completed against a real backend job, driven by a real Azure Document Intelligence call. The component tests prove the sequential-upload and polling-backoff *logic* is correct against mocked responses; nothing in this session exercised it against the real pipeline, for the same reason Stage 2's populated Dashboard view wasn't either — no browser tool, and real Azure credits weren't spent unprompted. **This gap is exactly what surfaced the bug in §6 below** — the thing the automated tests couldn't exercise is precisely where the real crash was.

## 6. Bug fix: "Maximum update depth exceeded" on upload (2026-08-08)

The user ran both apps locally and hit a real crash uploading a document — confirmation that §5's "not verified" gap above was load-bearing, not academic. Told to diagnose root cause (not patch superficially) and specifically to determine whether it was effect dependencies, unstable callback identities, or unnecessary reducer dispatches.

**Root cause, precisely**: `PolledUploadQueueItem`'s effect (`useEffect(() => { if (data) onStatusUpdate(data); }, [data, onStatusUpdate])`) called `onStatusUpdate` on every poll. `onStatusUpdate` was a fresh closure every render at *two* levels — `UploadQueueList` rebuilt a per-item `(job) => onStatusUpdate(item.id, job)` wrapper, and `UploadWorkspace` rebuilt the dispatch call itself, both inline. Meanwhile `uploadQueueReducer`'s `job-status-updated` case unconditionally built a new array via `.map()` regardless of whether the incoming job data actually differed from what was already stored. Chain: poll resolves → effect fires → dispatch → new array (even if nothing changed) → re-render → new callback identity → effect fires again — with no reference-equality check anywhere to let `useReducer` bail out.

Confirmed this redundancy was real, not just plausible, via direct instrumentation: a throwaway `console.log` inside the reducer (removed before committing anything), counting invocations while a job sat at a constant "processing" status. Measured ~1.75x more `job-status-updated` dispatches than real polls justified with one item under plain conditions, exactly 2x under `React.StrictMode` (matching its documented double-invoke behavior, not additional evidence of a worse bug), and linear (not multiplicative) scaling with a second simultaneously-polling item.

**Honest limitation, stated rather than hidden**: attempted an integration-level reproduction of the literal "Maximum update depth exceeded" crash in `UploadWorkspace.test.tsx` — five variations (plain, `StrictMode`, freshly-varying poll responses to defeat React Query's default structural sharing, two simultaneous items) — none crossed React's actual update-depth threshold inside jsdom/Vitest, despite the underlying redundant-dispatch pattern being real and measured. Rather than force a fragile repro or quietly ship something that looked like proof but wasn't, the regression tests target the precise, deterministic property instead (below), and the integration test that remains is documented in its own comment as a sanity check, not primary proof.

**Fix**:
1. **Primary**: `job-status-updated`'s reducer case now returns the *exact same* state array reference (no new array, no new item object) when the incoming job's `status`/`errorMessage` match what's already stored for that item, or when the target id isn't in the queue at all. This is what actually lets `useReducer` stop scheduling re-renders — an equal-but-new array every time (the old behavior) never gave React the chance.
2. **Secondary**: `PolledUploadQueueItem` now accepts `onStatusUpdate: (id, job) => void` and curries `item.id` itself (already has it as a prop) instead of `UploadQueueList` pre-currying a fresh closure per item per render; `UploadWorkspace` wraps its handler in `useCallback` keyed on `queue.dispatch` (stable — `useReducer`'s own dispatch never changes identity), deliberately not on the whole `queue` object (a fresh reference every render, which would have defeated the memoization entirely — confirmed by momentarily trying it and watching `react-hooks/exhaustive-deps` flag exactly this).

**Tests**: `queue.test.ts` gained three reducer-level cases — unchanged status/errorMessage returns the same reference (`toBe`), a real change (different `errorMessage`) still returns a new one, and an unknown id also returns the same reference (tightening prior coverage from value-equality to reference-equality). `UploadWorkspace.test.tsx`'s existing polling-regression test was simplified (dropped the `StrictMode` wrapper and two-file variant added during diagnosis, since neither changed the outcome) and its docstring rewritten to say plainly what it does and doesn't prove. `PolledUploadQueueItem.test.tsx`'s one assertion on the old `onStatusUpdate(job)` signature was updated to `onStatusUpdate(id, job)`.

**Verification**: full workspace `typecheck`/`lint`/`test` green, 94 frontend tests (up from 90). Re-ran the real curl-based `/upload` check — confirms no server-rendering regression, but this specifically **cannot** verify the fix itself, since the bug has no server-rendered signature at all. This one needs the user's own browser to actually confirm.

**Confirmed fixed**: the user re-tested in a real browser and reported back directly — uploaded a document, no more "Maximum update depth exceeded," no React warnings in DevTools, upload completed and polling behaved correctly. That's the real verification for this bug; everything above was the closest available substitute until it landed.

## 7. Stage 4: Results (2026-08-08)

Built via the same incremental TDD loop as Stage 3 (§5) — types/contracts, failing tests, minimal implementation, full-workspace verify, one slice at a time, never batching unverified work. 46 new tests (140 total, up from 94).

### Data layer: `features/results/extract-fields.ts`

`extractDocumentFields(resultJson: Record<string, unknown> | null)` walks `resultJson.documents[].fields`, returning `{name, displayValue, confidence: number | null}[]` — `displayValue` prefers a field's `content` (string) over `value` (string or number, stringified), falling back to `"—"` if neither is usable; `confidence` is `null`, never a fabricated `0`, when the field doesn't carry a numeric one (mirrors the backend's own `null`-not-`0` convention, §4 project-root decision 6). `extractRawContent(resultJson)` returns `resultJson.content` when present — the case for `generic` (Azure's `prebuilt-layout` model) results, which have no `documents[].fields` structure at all, only `{pages, tables, content}`.

Deliberately does **not** model Azure's full `DocumentField` union (bounding boxes, nested object/array fields, per-type value shapes) — every access goes through an `isRecord` guard and degrades to skipping that field/document rather than throwing on anything malformed or unexpected. This mirrors `apps/backend/src/utils/confidence.ts#computeAverageConfidence` deliberately: that's the one place in the codebase that already has to defensively parse this same genuinely-untyped field, and its own test fixtures (`{documents:[{fields:{Name:{value,confidence}}}]}` for field-shaped results, `{pages,tables}` for generic) are what this session verified the extraction logic's shape assumptions against, in the absence of a real Azure Document Intelligence response to check against directly (root `PROGRESS.md` §4 decision 26 — a real gap, not silently treated as fully closed).

16 tests: null input, non-array `documents`, real invoice/receipt-shaped extraction, multi-document flattening, `content`-over-`value` preference, the value-fallback and em-dash-fallback paths, null confidence for both missing and non-numeric values, malformed-field/malformed-document skipping without throwing, a `generic`/layout result correctly returning `[]`, plus 5 more for `extractRawContent` (null, present, missing, empty-string-treated-as-absent, non-string-treated-as-absent).

### Preview: `preview.ts`, `preview-cache.ts`, `use-document-preview.ts`

`preview.ts` implements the `DocumentPreviewSource` union locked in during Stage 3 planning (project-root `PROGRESS.md` §4 decision 2: `session-blob | remote-url | unavailable`) — `remote-url` still has no producer, since the backend still never persists uploaded files; the type exists purely so one can slot in later without touching any UI component that consumes it.

`preview-cache.ts` is a plain module-level `Map<string, File>` — deliberately not React state, not a Context, not lifted into `queue.ts`. It exists for exactly one purpose: letting `/documents/[jobId]` (a separate route from `/upload`) recover the just-uploaded `File` after a client-side navigation away from the upload queue's own component tree, where that `File` otherwise only lives in local reducer state. A plain module `Map` survives client-side route changes within the same page load (the module isn't re-evaluated) but is gone after a real page reload — matching the already-decided "session-only preview" scope exactly, since there's no cross-session story to build toward anyway. 4 tests, each using a unique `jobId` since the cache is genuinely module-level and persists across tests within the file (see §8 below for a test-authoring mistake this caused and how it was caught).

`use-document-preview.ts#useDocumentPreview(jobId)` reads the cache in a `useEffect`, creates a blob URL via `URL.createObjectURL` when a file is found, and revokes it in the effect's cleanup — on unmount and whenever `jobId` changes, so switching between two cached files doesn't leak the first one's URL. 4 tests: `unavailable` when nothing is cached, a `session-blob` result (`url` starting with `"blob:"`) when something is, `URL.revokeObjectURL` called with the right URL on unmount (`vi.spyOn`), and the previous URL revoked + a fresh one created when `jobId` changes via `rerender`.

**Wired into the upload flow**: `use-upload-controller.ts`'s `uploadMutation.mutate(...)` call now calls `cacheFileForPreview(response.job.jobId, next.file)` inside `onSuccess`, only for `response.kind === "single"` — a `.zip` batch's individual entries were never separately accessible client-side to begin with (same limitation `queue.ts` already documents for live sub-job polling), so there's no per-file `File` to cache against any of them. 2 new tests in `use-upload-controller.test.tsx`: a single upload's file is cached under its job id; a batch upload caches nothing.

### A lint rule, investigated rather than blindly suppressed or blindly obeyed

`react-hooks/set-state-in-effect` — part of the currently-installed `eslint-plugin-react-hooks@7.1.1`'s React-Compiler-oriented rule set (pulled in transitively through `eslint-config-next`; not something bumped this session) — flagged `useDocumentPreview`'s effect for calling `setSource(...)` directly in its body. Rather than either silently adding a blanket disable or restructuring around it without checking whether the rule had a real point, read the rule's own implementation (`validateNoSetStateInEffects` in the installed package's compiled source) to see exactly what it flags and what it recommends instead. It names three remedies: non-local derived data (doesn't apply — nothing here is owned by a parent), a derived-event pattern (doesn't apply — no prop transition being detected), and `useSyncExternalStore` for syncing from external mutable state (closer — `preview-cache.ts`'s `Map` genuinely is exactly that).

Worked through `useSyncExternalStore` concretely and it doesn't fully fit: it can cleanly replace the *read* half (getting the cached `File` for a `jobId`), but this hook also needs to *create a resource with paired cleanup* (the blob URL), which `useSyncExternalStore`'s `getSnapshot` contract doesn't cover — `getSnapshot` needs to be a pure, repeatable read, not something that allocates a browser resource each time it might get called (React can call it speculatively). The render-time alternatives that could compute the URL without an effect — `useMemo`, a lazy `useState` initializer — have a sharper problem: React Strict Mode's development-only double-invoke calls render-phase code (including `useMemo` callbacks and lazy initializers) twice with no paired cleanup mechanism, so either would leak one `blob:` URL per mount in development. The original `useEffect`-based pattern doesn't have this problem, because Strict Mode's double-invoke for *effects* specifically is mount→cleanup→mount — the first blob URL gets revoked before the second is created. Concluded the original pattern is not just acceptable but actually the *safer* one of the options considered, and kept it, with a single `eslint-disable-next-line react-hooks/set-state-in-effect` on the one line the rule actually flagged (the `unavailable`-reset branch — the `session-blob` branch's own `setSource` call, it turns out, isn't flagged by this rule at all; confirmed by briefly adding a disable comment there too and seeing ESLint itself report it as unused) and a comment explaining the reasoning above, not just "rule doesn't apply here."

### Components: `components/results/`

- `DocumentFieldsTable` — a `<table>` of name/value/confidence rows (`formatPercent` for a numeric confidence, `"—"` for `null`); renders `null` for an empty `fields` array rather than an empty table, so the caller (`DocumentResultsView`) can decide what replaces it. 4 tests.
- `DocumentRawContent` — the `generic`-document case: a `max-h-96 overflow-y-auto` block with `whitespace-pre-wrap` text; renders `null` for `content === null`, same empty-case convention as `DocumentFieldsTable`. 2 tests.
- `DocumentPreviewPanel` — wraps `useDocumentPreview(jobId)`; a `session-blob`/`remote-url` source renders through a single, uniform `<iframe title="Document preview">`, not a type-branched `<img>` vs. PDF-viewer split — `DocumentPreviewSource` deliberately carries no MIME type, and browsers already render both images and PDFs inside a frame natively, so branching would only add a prop for a marginal gain. `unavailable` renders a small "Preview unavailable" message. 2 tests, using the real `preview-cache`/`useDocumentPreview` rather than mocking either (consistent with this codebase's existing preference — see §5's testing-boundary note — for mocking only true I/O, and these two are fast, synchronous-enough, and already independently tested).
- `DocumentResultsView` — the composition point. Calls `useJobStatus(jobId)` (Stage 3, unchanged) for live, backoff-polled status. Handles, as distinct rendered states: loading (no data yet — a couple of `Skeleton` blocks plus visible "Loading…" text), error (the query itself failed — an `Alert`), `pending`/`processing` (a spinner + status text; polling continues automatically, nothing extra needed here), `failed`/`rejected_quota` (an `Alert` with the job's `errorMessage`, and a status-specific title distinguishing the two), and `completed` — average confidence (if non-null), then `DocumentFieldsTable`/`DocumentRawContent`, with a "No extracted data available." fallback for the edge case where a completed job's `resultJson` is genuinely `null` (possible per the backend's own `resultJson = azureStatus.analyzeResult ?? null`, §3 backend-integration notes above), alongside `DocumentPreviewPanel` in a two-column layout on larger screens. 9 tests, one per meaningful state plus the fallback and the query-error case.

### Route: `app/(dashboard)/documents/[jobId]/page.tsx`

Resolves the routing question the project-root `PROGRESS.md`'s own §9 had left "genuinely open" going into this stage (candidates were a dedicated `/documents/[jobId]` route or folding into a future History per-row view) — `/documents/[jobId]` was chosen, both because it's what this stage's own `preview-cache.ts`/`use-document-preview.ts` doc comments already assumed while being written, and because a job-detail route reachable directly from the upload queue is the more natural flow than requiring a detour through an unbuilt History page first. A thin `async` Server Component (same split as `/upload/page.tsx`) — awaits Next 16's `params: Promise<{jobId: string}>`, renders a static `PageHeader` ("Document results" — the real file name is only known after `DocumentResultsView`'s client-side fetch resolves, and duplicating that fetch server-side just for a title wasn't judged worth it) and `<DocumentResultsView jobId={jobId} />`.

### Reachability: linking out from the upload queue

`UploadQueueItemRow` gained a "View results" link (`Button variant="link" size="xs" asChild` wrapping a `next/link` `Link` to `` `/documents/${item.jobId}` ``), shown only when `item.status === "completed" && item.jobId` — without this, the new route existed but was reachable only by typing a URL by hand. Gated on `"completed"` specifically rather than "as soon as a `jobId` exists" (which would also work, since `DocumentResultsView` already handles `pending`/`processing` gracefully) — the more conservative of two reasonable choices, and not what was asked for, so left for later if wanted. 3 new tests: the link appears with the right `href` for a completed item with a `jobId`, and is absent both for a completed item with no `jobId` (defensive — not a reachable state via any real reducer action today, but the type allows it) and for a non-completed item.

### Verification

Full workspace `typecheck`/`lint`/`test` clean after every slice (extraction, preview cache, preview hook, upload-controller wiring, each of the four components, the route) — 46 new tests, 140 total, 234 with the backend's 94.

**No browser tool available this session either, and unlike every prior stage, no `curl`-based fallback check was attempted this time.** Results' actually-meaningful states — `pending`/`processing` with a real in-flight job, `completed` with real extracted fields and a real preview — all need a real, already-processed document job to exist first, which needs either a real Azure Document Intelligence call or a database seed (the same constraint that already left the Dashboard's populated view and Stage 3's interactive upload flow unverified, §4 and §6 above). Rather than fetch just the route's bare HTML shell via `curl` (which would prove little beyond "it doesn't 500 with no job data," already covered by the `isError` test) the user chose to verify this stage directly, manually, in a real browser — before this document was even updated. **As of this commit, Stage 4's correctness rests entirely on the automated test suite; the one class of bug this project has already hit once (§6 above) is exactly the kind that suite cannot see.**

*(Confirmed in a real browser in a later session — no discrete update was made here at the time, since the very next work was the architecture-documentation pass and then Stage 5 directly. Inferred from Stage 5 having subsequently been authorized and built, which this project's own established gate wouldn't have allowed otherwise. See the root `PROGRESS.md` §2 for the same note.)*

## 8. Stage 5: Document History (2026-08-07)

Built via the same incremental TDD loop as Stages 3-4 — types/contracts, failing tests, minimal implementation, full-workspace verify, one feature category at a time. Also backfilled two pieces of previously-zero test coverage found along the way: the backend's `listDocuments`/`GET /documents` path and this app's own list-fetching plumbing, neither of which had dedicated tests despite existing since Stage 2. 94 backend + 197 frontend tests by the end (up from 94/140).

### Data layer: `features/documents/`

`filters.ts` — `DocumentsFilters` (status/documentType/search/dateFrom/dateTo), `DocumentsListState {filters, cursors}` where `cursors` is a **stack** of prior page cursors, not just the current one, so "Previous" can walk backward through keyset pages without the backend needing to support reverse pagination (it doesn't — `documents.repository.ts#listDocumentJobsForOrganization`, Stage 2, is forward-cursor-only, unchanged). `documentsListReducer` handles four actions: `set-filter` (also resets the cursor stack — changing a filter invalidates whatever page you were on), `reset-filters`, `next-page` (pushes the just-returned `nextCursor`), `prev-page` (pops the stack). `currentCursor(state)` reads the top of the stack. `query.ts#buildDocumentsListPath` turns `{filters, cursor}` into the exact query-string shape `GET /documents` has accepted since Stage 2 — this stage added zero backend surface, purely a new consumer of an already-tested endpoint. `hooks.ts#useDocumentsList` wraps it in a `useQuery` keyed `["documents-list", params]`.

### `hooks/use-debounced-value.ts`

A new, generic `useDebouncedValue<T>(value, delayMs)` — not documents-specific, deliberately placed at the top-level `hooks/` (not `features/documents/`) since nothing about it is domain-specific. The filter bar's free-text search input debounces through this before it ever reaches `DocumentsFilters`, so typing doesn't fire one request per keystroke.

### Components: `components/documents/`

- `DocumentsFilterBar` — status/type `<select>`s, the debounced search `<input>`, a date-range pair, and an always-visible "Clear filters" button (see the bug below for why "always-visible" specifically matters).
- `DocumentsTable` — a plain `<table>`, matching `DocumentFieldsTable`'s Stage-4 precedent (see below for why `@tanstack/react-table` still isn't used).
- `DocumentsPagination` — Previous/Next, driven by the cursor stack; Previous disabled at the stack's bottom, Next disabled once the API stops returning a `nextCursor`.
- `DocumentsWorkspace` — composition point, replacing the Stage-1 stub at `/documents`.

### `@tanstack/react-table` still isn't used

Installed since Stage 0, still unused after Stage 4 and now Stage 5. On inspection, the actually-installed major version (9.0.0) has a materially different, more complex API (`useTable` + `TableFeatures` + store atoms) than the familiar v8 API (`useReactTable`/`getCoreRowModel`/`createColumnHelper`) that both the original plan and this stage's own first instinct assumed. Given this table needs no sorting, selection, or expansion, plain HTML markup — the same call `DocumentFieldsTable` already made — was judged the better fit both times: a deliberate architectural choice, confirmed by a second real instance, not a one-off workaround.

### A real UX bug, found during the build

Once a filter was active and returned zero results, **two** "Clear filters" buttons were simultaneously visible with identical labels — the filter bar's own always-present button, and a second, redundant action button inside the empty state. Beyond being visually confusing for a real user, it broke a straightforward `getByRole("button", {name: /clear filters/i})` test query outright ("found multiple elements"). Fixed by removing the empty-state's own action button — the filter bar's is already visible above it in every case where that empty-state message can even show.

### A fake-timer + `act()` gotcha, distinct from Stage 3's `waitFor` issue

`use-debounced-value.test.ts`: a bare `await vi.advanceTimersByTimeAsync(...)` correctly fired the underlying `setTimeout`, but the resulting `setState` never showed up in `renderHook`'s `result.current` afterward. Ruled out an implementation bug first — the identical failure reproduced whether the fake time was advanced in one large jump or many small steps, which wouldn't be true if the debounce's own timing logic were wrong. Root cause: a hand-rolled `useEffect`+`setTimeout`+`useState` pattern's timer-driven update needs its fake-timer advancement explicitly wrapped in `act(async () => {...})` to be reflected — distinct from `useJobStatus`'s own `refetchInterval` (TanStack Query's internal scheduling), which doesn't need this wrapping despite also being timer-driven. Fixed by wrapping each `vi.advanceTimersByTimeAsync` step in `act()`; documented directly in the test file's own comment, since this is exactly the kind of environment gotcha worth not rediscovering next time.

### Verification

Full workspace `typecheck`/`lint`/`test` green — 94 backend + 197 frontend tests. **Confirmed in a real browser** by the user: filters, search, pagination, empty states, navigation, and the browser console all checked directly — and two real, separate issues were found during that pass, reported immediately per this project's established pattern rather than after PROGRESS.md/commit. See §9.

## 9. Two bugs from Stage 5's manual verification (2026-08-07)

Investigated with an explicit instruction to find root cause, not patch symptoms, before touching PROGRESS.md or committing anything — same discipline as §6's crash investigation.

### Issue 1 — "Preview unavailable" reached via History or a page reload

Investigated the entire preview pipeline end to end: the upload flow's `cacheFileForPreview` call, `preview-cache.ts`'s module-level `Map`, the `DocumentPreviewSource` union, `useDocumentPreview`'s blob-URL creation/cleanup. Conclusion: this was the correct, honest behavior of a real, already-documented Stage-4 limitation (§7 above) — the preview cache is genuinely session-only, browser-memory state, and a job reached via History (a separate navigation from the immediate post-upload flow) or a fresh page load has nothing cached to show. Not a bug in code that existed — a real product gap in what that code was ever scoped to cover. This conclusion is what triggered §10's redesign; the fix wasn't a better "unavailable" message, it was persisting the file for real.

### Issue 2 — garbled raw-content text for `generic` documents

Root cause traced to the backend, not the frontend: Azure was never asked for its Markdown output format, so `resultJson.content` was flat, reading-order-only text — the table/section structure Azure's own model *does* capture (in `pages[]`/`tables[]`) was discarded on the way to that flat string, leaving nothing for a plain-text rendering to preserve. Backend fix: `azure-document-intelligence.service.ts#beginDocumentAnalysis` gained an optional `outputContentFormat?: "markdown"` parameter; `documents.strategy.ts`'s `generic` strategy entry (only) now passes it — invoice/receipt/identity_document don't, since their `content` isn't the primary thing shown to the user and this hadn't been verified against a live Azure response for those specific models. On this app's side, `DocumentRawContent`'s text rendering got a minimal, deliberately temporary readability fix in the same pass — switched to a monospace font, so a Markdown table's `|`/`-` characters at least read as a rough grid — fully superseded by §11's real Markdown rendering a few steps later; kept only long enough to bridge to that redesign.

Both issues together are what prompted the user to explicitly pause new feature work and request an architecture redesign instead of further incremental patches — see §10-§12.

## 10. Architecture redesign, Part 1: persistent document storage (2026-08-07)

Framed explicitly as intentional architecture evolution, not a bug fix: move the original uploaded file from session-only browser memory to real backend-persisted Supabase Storage, so it's retrievable from any screen, not just the tab that did the upload. Planned together with two other pieces (Part 2, §11, and a file-lifecycle design later absorbed into a larger redesign, §12) via Plan Mode; sequencing (plan all three, implement Part 1 first) and the eventual delete model (soft-delete job rows, hard-delete only the file) were both confirmed with the user via `AskUserQuestion` before any code.

### Storage & schema

Migration `0011_document_storage.sql` (backend, `apps/backend/supabase/migrations/`) — this app doesn't own migrations, but the schema shape is directly relevant to what the frontend now consumes: a private `documents` Storage bucket, `document_jobs.storage_path text` (nullable — null means no file persisted, either predating the migration or a non-fatal upload failure), and a `storage.objects` RLS policy as defense-in-depth (real enforcement stays the backend's service-role client, same model as `document_jobs` itself).

### Backend surface this app now consumes

A new `GET /jobs/:id/preview-url` → `{url: string | null}`, deliberately separate from the job status DTO rather than a field on it — embedding a signed URL in `JobDto` would mean generating one on every routine status *poll* while a job is still processing, when there's nothing to preview yet.

### `use-document-preview.ts`, rewritten

Previously a `useEffect` reading only the session cache (§7). Now a `useQuery` against the new `preview-url` endpoint (BFF route: `app/api/documents/[jobId]/preview-url/route.ts`, mirroring `[jobId]/route.ts`'s existing shape exactly), plus the session cache retained as a secondary fast-path/fallback:

- A resolved signed URL always wins when present — the persisted file is the real source of truth now.
- The session cache (`preview-cache.ts`, otherwise unchanged) is consulted only for an instant render while the signed-URL fetch is still in flight, or as a last resort if server-side storage genuinely failed for that job.
- It is never consulted once the backend has positively confirmed one way or the other, and never overrides a real signed URL.

**A real gap found and fixed proactively while implementing this, not present in the original plan**: without care, the hook would return `unavailable` both while the signed-URL query was still loading *and* once genuinely confirmed empty — meaning a reloaded or History-opened job would flash "Preview unavailable" for a moment before its real preview arrived. Fixed by adding a fourth variant, `loading`, to `DocumentPreviewSource` (now `loading | session-blob | remote-url | unavailable`, up from three at Stage 4), using TanStack Query's `isPending` specifically to distinguish "don't know yet" from "confirmed nothing." `DocumentPreviewPanel` gained a matching `<Skeleton>` branch; its "unavailable" copy was updated (it used to explicitly say "only available in the same browser tab," which is no longer true for anything uploaded after this migration).

Pre-existing jobs (uploaded before `0011`) have `storage_path = null` permanently and fall back to session-cache-or-unavailable exactly as before — stated plainly, not silently glossed over, since there's no original file left anywhere to backfill.

### Verification

Full workspace `typecheck`/`lint`/`test` green. **Confirmed in a real browser**: the uploaded file appears in the actual Supabase Storage bucket; the Results-page preview loads via a real signed URL (checked the `<iframe>`'s `src` in DevTools — not a `blob:` URL); survives a full page reload; also works reaching the same job via the History table — the two specific cases Issue 1 (§9) identified as broken.

## 11. Architecture redesign, Part 2: Results-page information architecture (2026-08-07)

Investigated before writing any code, per explicit instruction, rather than jumping straight to "add a Markdown renderer." Confirmed directly from source that `result_json` is Azure's entire response, stored verbatim/untyped — `paragraphs[]`, `tables[]`, `pages[]` (word/line bounding boxes) all arrive and are all persisted, but this app has only ever read two things out of it: `documents[].fields` and the flat `content` string. `generic` documents have no `documents[].fields` at all.

### Three options weighed for the raw-content panel's canonical representation

(a) Render Azure's own Markdown `content` properly. (b) Bypass `content` entirely and reconstruct a custom viewer directly from `paragraphs[]`/`tables[]`, which carry `role` (title/sectionHeading/etc.) and bounding regions that a flat string doesn't — real future value for a positional "click a field, see it highlighted on the source" review UI. (c) something else.

**Rejected (b) explicitly, for this pass**: Azure's own Markdown conversion already does the reading-order paragraph/table interleaving correctly; there's no live Azure response sample anywhere in this repository to validate a hand-built reconstruction against; and nothing is actually lost by deferring it, since the full untouched payload stays persisted in `result_json` regardless of which rendering approach runs today — a future structured/positional viewer remains just as buildable later, off the same stored data. Went with (a).

### A second, real bug this investigation surfaced

`DocumentResultsView` was unconditionally rendering `DocumentRawContent` whenever `content` was non-null, for **all four** document types — meaning invoice/receipt/identity_document jobs were showing their fields table *and* a full raw-text dump at equal visual prominence, uncollapsed. This directly contradicted the backend's own `documents.strategy.ts` code comment, which claimed those three types' content "isn't surfaced in the UI." The comment was simply stale relative to what this component actually did; the behavior needed fixing regardless of what the comment claimed.

### Implementation

Added `react-markdown`+`remark-gfm`+`@tailwindcss/typography` (none previously installed; the typography plugin wired into `app/globals.css` via `@plugin "@tailwindcss/typography";`, Tailwind v4's CSS-based plugin registration). `DocumentRawContent` gained a `format: "markdown" | "text"` prop — the Markdown pipeline (`<ReactMarkdown remarkPlugins={[remarkGfm]}>` inside a `prose prose-sm dark:prose-invert` container) applies only when `format === "markdown"`; `"text"` keeps the prior preformatted/monospace rendering byte-for-byte unchanged. This split is a correctness matter, not just presentation: only `generic` documents actually receive Markdown from Azure (§9) — running the other three types' plain OCR text through a Markdown parser risks misinterpreting incidental characters (a line starting with `"- "`, a stray `#`/`_` in an address or ID number) as formatting instead of literal content. A new `extract-fields.ts#isMarkdownContent(documentType)` decides which format applies, hand-mirroring the backend's own per-type routing table (`documents.strategy.ts`) — the same hand-kept-in-sync, dated-comment convention `types/api.ts` already uses for `DOCUMENT_TYPES` itself.

`react-markdown`'s default behavior never renders raw HTML embedded in the source as live elements (`rehype-raw` was deliberately not added) — confirmed with a dedicated test, since this Markdown ultimately originates from a scanned document, not a trusted author, and this is exactly the kind of assumption worth verifying rather than taking on faith.

Fixed the stale-comment bug above by collapsing `DocumentRawContent` behind a native `<details><summary>Show raw extracted text</summary></details>` whenever structured fields also exist, leaving it at full prominence only for `generic` (where it's the only extracted content there is). No new dependency — a native disclosure element, consistent with this app's existing native-element-over-new-primitive precedent (e.g. `DocumentTypeSelect`'s plain `<select>`, Stage 3).

### Verification

Full workspace `typecheck`/`lint`/`test` green. **Confirmed in a real browser**: a `generic` document's raw-content panel renders real headings and a bordered table instead of literal `#`/`|` characters; an invoice/receipt/identity_document's raw content is now collapsed behind "Show raw extracted text," with the underlying text itself unchanged once expanded.

## 12. Document review workflow redesign — Phase 1 (2026-08-07)

After Part 2 was confirmed, the user reported the remaining gap wasn't a rendering problem: the Documents list only exposes a "View" action, and the Results page is read-only with no way to correct a wrong extraction or record that a human reviewed it. Requested a full architecture proposal — review, field editing, confirmation/rejection, Storage lifecycle, future audit/history, multi-document-type support — before any code.

### Planning

Investigated the current architecture directly (routes, schema, repository/service functions, existing frontend patterns — confirmed `components/ui/dropdown-menu.tsx` already exists and is already used for a multi-action row in `UserMenu.tsx`; confirmed no `Dialog`/`AlertDialog` primitive exists anywhere yet; confirmed `react-hook-form` is used only for the two auth forms) via Plan Mode, using a Plan subagent to draft and independently verify a proposal against real source before it was presented back. Two decisions were confirmed via `AskUserQuestion`:

- **Field corrections get a full, per-edit audit-history table**, not a simpler "latest value only" column — chosen specifically because the user named future audit/history as a first-class requirement, and the lighter option would have permanently lost any intermediate edit the moment it was superseded by a later one.
- **Any organization member — not just the uploader or an owner/admin — can edit fields, save corrections, confirm, or reject.** Matches how viewing already works (org-scoped, not uploader-scoped); destructive actions (remove the original file, delete the document — Phase 2, not yet built) stay restricted to the uploader or an owner/admin, the already-agreed model from an earlier, smaller file-lifecycle design that this larger redesign absorbed as its own later phase.

The approved plan sequences three phases — **Phase 1 (this section)**, Phase 2 (file lifecycle), Phase 3 (Documents-list fast-follow: a delete/remove-file menu, a review-status badge). Only Phase 1 is built.

### Data layer: `extract-fields.ts#extractEffectiveFields`

Merges Azure's originally-extracted fields (`extractDocumentFields`, Stage 4, unchanged) with the new corrections endpoint's effective-value map into `{name, originalValue, effectiveValue, isCorrected, confidence}[]`. `isCorrected` compares the *current* effective value against the original, not "does any correction exist for this field name" — a field corrected and then manually reverted back to Azure's original value correctly reads as uncorrected again, matching the backend's own audit-log semantics (a correction event still exists in `document_field_corrections`, but nothing about the *current* display should claim the field is still edited). `confidence` always stays Azure's original score even for a corrected field — a human override has no confidence of its own to report, so `DocumentFieldsTable` shows "Edited" in its place rather than a now-stale percentage.

### `features/results/hooks.ts` (new)

`useFieldCorrections(jobId, enabled)` — a `useQuery` against the new `GET .../corrections` endpoint; `enabled` is meant to be driven by the caller's own job-status check (`job?.status === "completed"`), since a job with no result yet has nothing to correct. `useSaveCorrections`/`useConfirmReview`/`useRejectReview` — three thin `useMutation` wrappers sharing one internal `useReviewMutation(path, method)` helper (same endpoint shape: `{corrections}` body in, a `JobDto` back), each invalidating `["job-status", jobId]`, `["field-corrections", jobId]`, and `["documents-list"]` on success — the last one specifically so the Documents list (once Phase 3 gives it a review-status badge) doesn't need its own separate invalidation plumbing bolted on later.

### `DocumentFieldsTable`, evolved from read-only to always-editable

Confirmed via a fresh grep that its only real consumer is `DocumentResultsView` (two other hits were doc-comment mentions, not imports) — safe to change its prop shape directly rather than needing a parallel component. Now takes `{fields: EffectiveField[], draft: Record<string,string>, onFieldChange}`; each row's value renders as a controlled `<input>` (shadcn's `Input`), not static text. **No separate view/edit-mode toggle** — the Results page's stated purpose is now a document review workspace where correcting a value is the core action, not an occasional one, so an extra click to "enter edit mode" first would be pure friction with no real benefit (nothing saves until "Save corrections"/"Confirm"/"Reject" is explicitly clicked, so there's no accidental-edit risk to guard against either).

### `DocumentResultsView`, the action bar

A full-width bar above the existing two-column fields/preview layout: a review-status badge (`Unreviewed`/`Confirmed`/`Rejected`, following the exact `{label, icon}` display-map convention `DocumentsTable`'s own `STATUS_DISPLAY` already established for processing status, Stage 5), "Save corrections" (enabled only when the local edit draft is non-empty; not rendered at all for `generic` documents, which have no fields), "Confirm," "Reject," with inline error feedback (an `Alert`) if any of the three mutations fails.

**`react-hook-form` was deliberately not reused here**, despite being this app's established pattern for its two other forms (login/signup). RHF earns its keep there through schema-driven validation (`zodResolver`) against a fixed, known-ahead-of-time field set. Neither holds for corrections: the field set is dynamic and Azure-determined (a different, variable-length list per document), and a corrected value has no real validation rule to enforce beyond "an arbitrary replacement string." A plain `useState` draft — `Record<string, string>`, diffed for a dirty check — covers what's actually needed without RHF's added surface area for schema-driven validation this form doesn't need.

### A lint-driven fix worth recording as a pattern

Resetting the local edit draft whenever `jobId` changes was first written as a `useEffect` calling `setState` — `react-hooks/set-state-in-effect` correctly flagged this as avoidable, unlike Stage 4's blob-URL case (§7), which genuinely needs an effect for its paired resource-cleanup semantics (create a blob URL, revoke it on cleanup — an external browser resource). This case has no external resource at all, just local state that shouldn't leak between two different jobs' review sessions — fixed using React's own documented "adjust state during render" pattern instead (a `draftJobId` comparison directly in the render body, resetting the draft synchronously within the same render pass), not another scoped disable comment. Worth remembering as the *other* half of the `set-state-in-effect` story this project has now hit twice: sometimes the effect is genuinely correct and the rule needs a documented override (§7's blob-URL case); sometimes it's flagging something structurally avoidable, and the fix should actually change the code.

### Verification

Full workspace `typecheck`/`lint`/`test` green — 146 backend + 250 frontend tests (up from 94/197). **Confirmed in a real browser**: editing a field and saving without confirming persists correctly across a reload while review status stays "Unreviewed"; confirming a document and then editing a field again correctly reverts its status to "Unreviewed" automatically; reject behaves the same way; `generic` documents correctly get confirm/reject with no field-editing UI; no console errors or React warnings throughout.

## 13. Document review workflow redesign — Phase 2: file lifecycle (2026-08-07)

The second of the three approved phases (§12) — remove the original file, or delete the whole document — absorbing the file-lifecycle design agreed before Phase 1 even started. Same TDD-first workflow and wait-for-manual-verification discipline as every phase before it.

### Backend surface this app now consumes

Two new endpoints: `DELETE /jobs/:id/file` (returns the updated `JobDto` — the job stays fully visible, so this matches confirm/reject's existing shape) and `DELETE /jobs/:id` (returns just `{jobId}` — deliberately not a full `JobDto`, since once deleted the job isn't retrievable through any other endpoint, so there's nothing else meaningful left to return). Both restricted server-side to the job's uploader or an org owner/admin — a stricter gate than Phase 1's any-org-member review actions, reflected in a new `deleteDocumentResponseSchema`/`DeleteDocumentResponse` type in `types/api.ts` for the second endpoint's leaner response.

### `services/documents.service.ts` and BFF routes

`removeDocumentFile(jobId)` and `deleteDocument(jobId)` added alongside the existing review-workflow functions. The `DELETE /jobs/:id/file` BFF route is a genuinely new file (`app/api/documents/[jobId]/file/route.ts`); the `DELETE /jobs/:id` BFF route is a new `DELETE` export added to the **existing** `app/api/documents/[jobId]/route.ts` (which, until now, only ever exported `GET`) — not a new file, since it's the same resource the `GET` handler already addresses.

### `features/documents/hooks.ts`: `useRemoveDocumentFile`/`useDeleteDocument`

Placed here, not in `features/results/hooks.ts` where Phase 1's review-workflow hooks live — deliberately, since Phase 3's Documents-list actions will need these same two hooks from a different page, not just the Results page. `useRemoveDocumentFile` invalidates `["document-preview-url", jobId]` on success — the one piece of cached data that actually changes, since `JobDto` never exposed `storage_path` to begin with. `useDeleteDocument` invalidates `["job-status", jobId]` and `["documents-list"]`.

### `components/ui/alert-dialog.tsx`: added via the shadcn CLI, not hand-written

`npx shadcn@latest add alert-dialog` — confirmed it produced a component matching this project's existing radix-nova conventions exactly (the same pattern already established by `dropdown-menu.tsx`), and that it touched only the one new file (it also tried to regenerate `button.tsx`, but skipped it since the content was already identical — verified via `git status`, not assumed). This is the first `Dialog`/`AlertDialog`-family primitive anywhere in this app; previously only `sheet.tsx` existed (the sidebar's mobile drawer), and there was no confirm-before-destructive-action pattern anywhere at all.

### `DocumentResultsView`: two new destructive actions

"Remove file" (`outline` button variant — milder, matching "Save corrections"'s existing treatment) and "Delete document" (`destructive` variant — matching "Reject"'s existing red treatment, since both are comparably serious), each behind its own `AlertDialog` confirmation, visually separated from Save/Confirm/Reject by a thin vertical divider rather than a new row (the plan called for "visually separated," and this was judged sufficient without a bigger layout change). Confirming "Delete document" calls `router.push("/documents")` afterward (`next/navigation`'s `useRouter`), since the job disappears from view the moment it's deleted — "Remove file" needs no navigation, the job stays exactly where it is. Both new mutations feed into the same inline error `Alert` and the same combined `isMutating` disable flag Phase 1 already built, extended rather than duplicated.

### A testing-pattern gap this phase had to fill

No prior test in this codebase exercised a Radix Dialog/AlertDialog-family component, so there was no established pattern to copy. Worked out two things the hard way, now documented here so they don't need rediscovering:
- Radix portals dialog content into `document.body` only once opened — it's genuinely absent from the DOM beforehand, not rendered-and-hidden — so `await user.click(trigger)` followed by `await screen.findByRole("alertdialog")` is the correct wait-for-open sequence, not a bare `getByRole` immediately after the click.
- A dialog's trigger button and its in-dialog confirm action can legitimately share the same accessible name (a deliberate, standard UX choice — both saying "Remove file," for instance, rather than an awkward mismatched pair). Once the dialog is open, both coexist in the DOM, so `screen.getByRole("button", {name: ...})` becomes ambiguous — tests need `within(dialog).getByRole(...)` scoping to pick out the one inside the dialog specifically.

Also needed this test suite's first `next/navigation` mock, since no earlier tested component called `useRouter` directly: `vi.mock("next/navigation", () => ({ useRouter: () => ({ push: mockPush }) }))`, with `mockPush` cleared in a module-level `beforeEach` (not scoped to one `describe` block, since more than one block in the same test file now needs it).

### Verification

Full workspace `typecheck`/`lint`/`test` green — 168 backend + 265 frontend tests (up from 146/250). **Confirmed in a real browser**: Remove file works correctly; Delete document works correctly; Storage behaves correctly; soft-delete behaves correctly; no regressions found; no console errors.

## 14. Document review workflow redesign — Phase 3: Documents-list actions (2026-08-07)

The third and final phase of the approved plan (§12). The Documents list (`DocumentsTable.tsx`, Stage 5, §8) still only had a single "View" link per row — no way to remove a file or delete a document without first navigating to that document's own Results page — and no visibility into review status from the list at all. Phase 3 closes both gaps. **No backend changes, no new migration** — unlike every phase before this one, this is pure frontend composition of what Phases 1 and 2 already built.

### New component: `components/documents/DocumentRowActions.tsx`

A `DropdownMenu` per row (View / Remove file / Delete document), replacing the table's previous bare `<Link>View</Link>`. Calls `useRemoveDocumentFile()`/`useDeleteDocument()` (from `features/documents/hooks.ts`, built in §13) — **one hook instance per rendered row**, matching the exact "hook per item in a dynamic list" pattern already established for `PolledUploadQueueItem` back in §5 — deliberately not one shared mutation instance at the table or workspace level, since that would mean all rows' pending/error state collapsing onto one shared object (row A's in-flight delete would incorrectly show row B's button as disabled too).

### A real Radix composition detail worth recording as a pattern

The confirmation dialogs here use `AlertDialog`'s *controlled* form (`open={confirmingAction === "remove-file"}` / `onOpenChange`), not the simpler `AlertDialogTrigger`-wraps-a-button pattern `DocumentResultsView.tsx` used for its own Remove file/Delete document actions in §13. This is a deliberate, necessary difference, not an inconsistency: here the trigger is a `DropdownMenuItem`, and nesting an `AlertDialogTrigger` directly inside a `DropdownMenuItem` is a known-fragile Radix composition (the dropdown menu's own closing/focus-return behavior on item-select can race the `AlertDialog` trying to open right after, sometimes stealing focus or closing the dialog prematurely). The fix: each destructive `DropdownMenuItem`'s `onSelect` handler calls `event.preventDefault()` (stops the menu's default close-and-refocus from interfering) and sets local state (`confirmingAction: "remove-file" | "delete" | null`); a separate, controlled `AlertDialog`, rendered as a sibling of the `DropdownMenu` rather than nested inside it, opens based on that state. Confirmed working via real interaction tests (`user.click` through the actual open → select → confirm sequence), not just rendered-output assertions.

### Review-status badge column

Added to `DocumentsTable.tsx`, between the existing `Status` (Azure processing) and `Confidence` columns. Reuses `REVIEW_STATUS_DISPLAY` — *exported* from `components/results/DocumentResultsView.tsx` for this purpose (it previously lived there as a private, unexported const from §12) rather than duplicated — the same cross-file small-display-map convention `DocumentsTable.tsx` already used for `DOCUMENT_TYPE_LABELS` (imported from `components/upload/DocumentTypeSelect.tsx`). Kept visually distinct from the existing `Status` column (a separate column, independently rendered) so a `rejected_quota` processing status ("Quota exceeded") and a `rejected` review status ("Rejected") never read as ambiguous or collide.

### Deliberately not built

The optional `reviewStatus` filter in `features/documents/filters.ts` — the plan itself marked this "optional, lowest priority... not required for Phase 3 to be considered done." Skipped consciously, not forgotten. Would follow the exact same reducer-entry pattern already used for `status`/`documentType` if picked up later.

### Verification

Full workspace `typecheck`/`lint`/`test` green — 168 backend tests (unchanged — nothing backend-side changed this phase) / 15 files, 273 frontend tests (up from 265) / 38 files. The user manually verified in a real browser: the row actions menu, review-status badge, and delete/remove-file behavior from the list were all confirmed working, matching the same behavior already verified from the Results page in §13.

**This completes the entire three-phase document review workflow redesign** — the plan that started from the user's "stop implementing new features and redesign this properly" request (§9). Phase 1 (§12) = field corrections (full audit-log history) + confirm/reject. Phase 2 (§13) = file lifecycle (remove file / delete document). Phase 3 (this section) = Documents-list actions. All three built, tested, and confirmed in a real browser.

## 15. Wave 1 — Documents-list UX fixes: accessible field names, error/pending-state handling (2026-08-08)

Frontend-specific detail for the Wave 1 pass described in full at the root `PROGRESS.md` §3.20 (companion-audit grounding, the full six-item scope, backend-side items). This session's frontend surface was two of the audit's named UX gaps, both in the document review workflow this file has been tracking since §12.

### Accessible field names — `DocumentFieldsTable.tsx`

Each field's `<Input>` (§12) had no `aria-label`, no associated `<label>`, and no `aria-labelledby` pointing at the adjacent field-name cell — a screen-reader user navigating by form field heard "edit text" with no indication of which one. Fixed with `aria-label={field.name}`. The new test deliberately queries by computed accessible name (`getByRole("textbox", { name: field.name })`), not by reading the `aria-label` attribute back off the DOM — the former actually exercises the browser's/jsdom's real accessible-name computation, the same resolution a screen reader performs; the latter would only prove an attribute string exists, which is a materially weaker claim.

### Silent-failure delete actions — `DocumentRowActions.tsx`

`removeFile.error`/`deleteDocument.error` (§14) were created by the mutation hooks but never read anywhere in this component — a permission error or any other failure produced zero user-facing feedback; the confirmation dialog just closed with nothing having happened, in contrast to the identical two mutations on `DocumentResultsView.tsx` (§13), which already surface failures via an inline `Alert`.

Fixed by mirroring that exact pattern here — a duplicated, proportionate `mutationErrorMessage` helper (same three-line duplication call already made for `mutationErrorMessage` itself between other files this project, not worth a shared import for), a destructive `Alert` — but placed *inside* each `AlertDialogContent` rather than at the page level, which only works because of something discovered while implementing it, not assumed going in: `AlertDialogAction` is Radix's `DialogPrimitive.Close` under the hood (confirmed by reading `@radix-ui/react-alert-dialog`'s actual source, not guessed), and closes the dialog on click by default — via `composeEventHandlers`, which runs the passed `onClick` first and only runs its own close-handler if the event *isn't* marked `defaultPrevented`. Both confirm handlers now call `event.preventDefault()` for exactly this reason: it stops Radix's default close, so the dialog — and the error inside it — stays open through a pending or failed mutation, closing only via the existing explicit `onSuccess` call. `onOpenChange` was also tightened to ignore a close request (Escape, outside-click) while that dialog's own mutation is still pending, so a user can't dismiss the dialog out from under an in-flight request.

Bundled into the same change, not a separate pass: pending-state handling on both dialogs (`disabled={isPending}` on Cancel/Confirm, "Removing…"/"Deleting…" labels replacing the static text) — the audit flagged this as its own, closely-related finding in the same file ("no pending-state feedback... a double-click during a slow request is currently possible") and explicitly suggested bundling it with the error-surfacing fix; doing so here also directly serves the same "keep the UI consistent with server state" property the error fix is for, since an un-disabled button during a pending mutation is a real double-submit risk, not just a cosmetic gap.

### Verification

Full workspace `typecheck`/`lint`/`test` green — 239 backend tests (up from 168 — auth/upload-security/ZIP work, no frontend-visible change) / 22 files, 277 frontend tests (up from 273) / 38 files: 1 new test on `DocumentFieldsTable.test.tsx` (accessible name), 3 new on `DocumentRowActions.test.tsx` (two error-surfacing cases, one pending-state case). **Manual browser verification for these two fixes has not yet happened** — no browser automation tool is available in this development environment (unchanged limitation, noted throughout this project's history); needed from the user before this would normally be considered fully done, per this project's established Definition of Done.

## 16. Billing — Phase 1: BFF + service layer (2026-08-08)

First frontend Billing work since Stage 2 left it deliberately minimal (§4 decision 17 in the root `PROGRESS.md`: `getSubscription()` only, `plans`/`checkout`/`portal` left for later). A full analysis of the existing backend, frontend, data model, security, and test coverage preceded this (not reproduced here — see the root `PROGRESS.md` §3.24 and the conversation it came from); five decisions were locked before implementation: checkout/portal restricted to `owner`/`admin` (backend-side, not this phase's concern), a simple refetch-on-redirect UX for the post-checkout return (Phase 3), Stripe's own hosted Portal for all plan-management (no custom switching UI), the existing usage-vs-downgraded-plan gap explicitly deferred, and backend test backfill included as Phase 0 (see `apps/backend/PROGRESS.md`).

This phase is BFF + service layer only — no UI, no hooks yet (Phases 2-3).

**`services/billing.service.ts`** gained `getPlans()`, `createCheckoutSession(input)`, `createPortalSession(input)` — same shape as the pre-existing `getSubscription()`: resolve the caller's access token via `getAccessToken()`, call `backendFetch` with the already-existing `plansResponseSchema`/`urlResponseSchema`. No new schemas — `checkoutRequestSchema`/`CheckoutRequest`, `portalRequestSchema`/`PortalRequest`, and `urlResponseSchema`/`UrlResponse` already existed in `types/api.ts`, fully defined since Stage 2 but with zero consumers until now.

**Three new BFF routes** (`app/api/billing/{plans,checkout,portal}/route.ts`), following `app/api/documents/route.ts`'s exact pattern — `parseJsonBody` for the checkout body, `toErrorResponse` in every catch block, no new helper. The BFF stays pure proxy/adapter; no business logic (plan validity, Stripe interaction) lives here.

**`redirectUrl`/`returnUrl` are computed server-side, per-request, inside each route** — `` `${new URL(request.url).origin}/billing` `` — never hardcoded, and never taken from a client-supplied value even though both request schemas technically allow one (`checkoutRequestSchema`/`portalRequestSchema` both have it as optional): the route always overrides whatever the client sent. Explicitly tested — one case sends a forged `redirectUrl` in the body and asserts it's ignored in favor of the request's real origin; another sends the request from a different origin entirely and asserts the computed URL follows it.

**Auth/org-context propagation is unchanged from the existing convention**: the BFF routes never touch cookies or tokens directly. `getAccessToken()` runs inside the service function; the backend's `authMiddleware` derives `organizationId`/`role` from the bearer token it receives. Nothing new needed here.

**Tests**: 14 new — 4 in `services/billing.service.test.ts` (new file; also closes the pre-existing gap that `getSubscription()` itself had never been tested, since the file didn't exist before this phase), 10 across the three new route test files (success + validation-failure + error-envelope-translation + the origin-derivation cases described above).

**Verification**: full workspace `typecheck`/`lint`/`test` clean — frontend 291 tests (up from 277), backend unchanged at 341 (Phase 0's count). No changes to `documents`/`worker` domains. Left uncommitted, pending review.

## 17. Billing — Phase 2: frontend hooks (2026-08-08)

`features/billing/hooks.ts` (new): `usePlans()` (a `useQuery` against `/api/billing/plans`, matching `useDocumentsList`'s exact shape) and `useCreateCheckoutSession()`/`useCreatePortalSession()` (`useMutation`s against the two Phase-1 BFF routes, matching `useRemoveDocumentFile`/`useDeleteDocument`'s shape — no extra wrapping beyond the raw TanStack Query mutation object, so `.isPending`/`.error`/`.mutate`/`.mutateAsync` all behave exactly as every other mutation hook in this codebase already does). `useCreateCheckoutSession`'s mutation input is `{ planId: CheckoutRequest["planId"] }`, not the full `CheckoutRequest` — `redirectUrl` is never sent from the client at all (Phase 1's BFF route always computes and overrides it server-side regardless), so exposing a field that would be silently ignored would just be a footgun.

**Both mutations' `onSuccess` redirects to the Stripe URL the BFF returns via `window.location.assign(url)`, not `router.push()`** — the first genuinely external (off-origin) navigation anywhere in this codebase; every existing mutation (`features/auth/hooks.ts`) redirects same-origin via the Next.js router, which cannot navigate off-domain at all. A shared `redirectToExternalUrl` helper backs both hooks so this isn't duplicated.

**A real, first-time testing obstacle, solved during this phase**: `vi.spyOn(window.location, "assign")` throws `Cannot redefine property: assign` — jsdom defines it non-configurable, unlike most DOM APIs. Fixed with `vi.stubGlobal("location", { assign: vi.fn() })` instead (vitest's own documented way around exactly this jsdom limitation), paired with `vi.unstubAllGlobals()` in `afterEach` so the stub can't leak into other test files run in the same worker — confirmed by running the full frontend suite afterward with no cross-file pollution.

**Tests**: 10 new in `features/billing/hooks.test.tsx` — `usePlans`'s fetch/loading/error states; both mutations' request shape, pending state, error state (and that a failed mutation never navigates), and — explicitly, per this phase's own requirement — that a successful mutation calls `window.location.assign` and *not* the mocked `next/navigation` router (`vi.mock("next/navigation", ...)`, the same module-level-spy shape already used in `DocumentResultsView.test.tsx`, not a new pattern).

No UI yet (`/billing` is still the Phase-0-era stub) and no checkout-success/cancelled handling — both are Phase 3.

**Verification**: full workspace `typecheck`/`lint`/`test` clean — frontend 301 tests (up from 291), backend unchanged at 341. No changes to `documents`/`worker`, no changes to the Phase 1 BFF routes. Left uncommitted, pending review.

## 18. Billing — Phase 3: `/billing` UI (2026-08-08)

Before this phase, a self-directed re-check of `billing.routes.ts` found the backend never actually enforced the owner/admin restriction Phase 0-2 had assumed — Phase 0 only backfilled tests for existing behavior, it never added the check. Fixed first, as its own small backend-only phase (`apps/backend/PROGRESS.md` §22: `assertCanManageBilling`, reusing the `ForbiddenError` pattern from `documents.service.ts#assertCanManageFile`), before any UI was built on top of it.

Seven TDD steps, each verified independently before moving to the next:

**Step 1 — `useSubscription()`** (`features/billing/hooks.ts`): a `useQuery` against `/api/billing/subscription`, matching `usePlans()`'s shape, with one addition — an optional `refetchInterval` passed straight through to TanStack Query's own option (not a bespoke polling mechanism); `false`/`undefined` disables it, which is the default for every caller except Step 6's bounded post-checkout window.

**Step 2 — status/plan display primitives**: `SubscriptionStatus`/`STATUS_LABELS`/`STATUS_VARIANTS` exported from `components/dashboard/CurrentPlanCard.tsx` (previously private to that file) so the same status vocabulary isn't redefined a third time. Two new presentational components, `components/billing/{PlanCard,CurrentSubscriptionCard}.tsx` — plain props/callbacks, no hooks of their own; all derivation happens once in `BillingWorkspace`. `PlanCard` derives `showAction` from `plan.price_monthly > 0 && !isCurrentPlan && !hasActiveSubscription` — checkout is only ever offered when the org has no real subscription at all, which is what makes "no custom plan-switching workflow, Stripe Portal only" concrete in the UI. Both disable their action button with an explanatory caption when `canManageBilling` is false (the confirmed decision: disable, not hide, for `member` role — backend stays the real authorization boundary regardless of what the UI shows).

**Step 3 — `BillingWorkspace` read-only shell**: composes `useSubscription()` + `usePlans()` with the two Step-2 components, following `DocumentsWorkspace`'s loading/error shape. `isRealSubscription()` discriminates a real `SubscriptionRow` (always has an `id`) from the synthetic implicit-free-tier response (never does) — this is what `hasStripeSubscription`/`hasActiveSubscription` are built on. `app/(dashboard)/billing/page.tsx` became an async Server Component fetching `role` via `getCurrentUser()` and passing it down as a prop — same rationale as `DashboardLayout`'s own doc comment (nothing needs a client-side refetch of the current user yet, and there's no `/api/auth/me` BFF route to fetch it from on the client regardless).

**Steps 4 & 5 — checkout/portal action wiring**: `useCreateCheckoutSession()`/`useCreatePortalSession()` wired into the two components' callbacks. Checkout pending/error state is scoped to the specific plan clicked (tracked via a `checkoutPlanId` bit of local state), not global — clicking Basic doesn't disable or show an error on Pro. `isCheckoutablePlanId()` narrows a plan's `string` id down to `CheckoutRequest["planId"]` (`"basic" | "pro"`) before calling `mutate`, matching the backend's own `checkoutRequestSchema` enum and `PlanCard`'s `price_monthly > 0` gate for the seeded free/basic/pro plans.

**Step 6 — post-checkout return handling**: the first use of `useSearchParams()`/`useRouter().replace()` anywhere in this frontend. On mount, `?checkout` is captured once (`useState(() => searchParams.get("checkout"))` — a checkout return is always a fresh external redirect, never a client navigation, so this doesn't need to react to later URL changes) and cleared via `router.replace("/billing", { scroll: false })` in the same effect that detects it, not deferred until any polling window completes. `?checkout=success` enables a bounded `refetchInterval` (2s, capped at 10 fetches — a `pollAttemptsRef` counted inside an effect that only runs once real data arrives) *and* a hard 20s `setTimeout` ceiling (covers a stalled/erroring fetch, which the attempt-counting effect can't see) — either one flips `checkoutPending` false, immediately stopping further polling and revealing the normal view with whatever was last fetched. While pending, a dedicated "Finishing up your subscription update…" state shows instead of the normal cards, reusing `DocumentResultsView.tsx`'s existing `LoaderCircle`-spin pattern rather than inventing a new visual language. `?checkout=cancelled` gets no such handling — just a non-destructive inline `Alert` above the normal (immediately-rendered) view. `app/(dashboard)/billing/page.tsx` wraps `BillingWorkspace` in `<Suspense fallback={null}>`, which Next.js requires around any `useSearchParams()` caller.

**Deliberately deferred, confirmed with the user before starting**: no usage meters on `/billing` (Dashboard's own usage display stays out of scope); the pre-existing "usage not re-checked against a downgraded plan" gap (unrelated to this phase, left untouched).

**Tests**: 39 new (4 `useSubscription` cases in `features/billing/hooks.test.tsx`, 8 each in `PlanCard.test.tsx`/`CurrentSubscriptionCard.test.tsx`, 19 in `BillingWorkspace.test.tsx` across its four `describe` blocks). `BillingWorkspace.test.tsx`'s post-checkout-return suite uses `vi.useFakeTimers({ shouldAdvanceTime: true })` plus a `vi.hoisted`-backed mock of `next/navigation`'s `useSearchParams`/`useRouter` (mutable per-test via `setSearchParams()`, since the module mock itself is hoisted above any per-test setup) — covers immediate resolution (webhook already landed by the time the page loads), resolution mid-poll, and full bound exhaustion with no webhook ever landing, in addition to the plain query-param-cleanup and cancelled-notice cases.

**Verification**: full monorepo `typecheck`/`lint`/`test` clean — frontend 340 tests (up from 301), backend unchanged at 347 (post-authorization-fix count). No changes to `documents`/`worker`, no changes to Stripe business logic, schema, or RLS. Left uncommitted, pending review.

**Manual Stripe Test Mode browser verification — performed by the user, all 5 flows PASS** (no browser-automation tool was available to the assistant; the user ran it directly, with the assistant preparing the environment first — see below):
- **Free-tier owner**: `/billing` loads correctly, Free/Active shown, Basic/Pro both offer enabled Select buttons.
- **Successful checkout**: real Stripe Test Mode Checkout completed, redirected back to `?checkout=success`, "Finishing up your subscription update…" shown, subscription correctly updated to Basic/Active once the webhook landed, `Manage billing` appeared, URL cleaned back to `/billing`.
- **Stripe Customer Portal**: `Manage billing` opened the real Customer Portal; returning preserved the correct Basic/Active state.
- **Cancelled checkout**: cancelling from Stripe returned to `?checkout=cancelled`, showed the non-destructive cancellation notice, no subscription change, URL cleaned back to `/billing`.
- **Member authorization**: `/billing` fully visible to a `member`, billing actions rendered disabled with the explanatory caption, and direct `POST /api/billing/{checkout,portal}` calls from the browser console both returned `403` — confirming the backend stays the real authorization boundary independent of the UI.

**Environment prep for the manual pass** (not part of the app itself): Stripe CLI installed via `npm install -g @stripe/cli` (user-owned prefix, no sudo needed) and confirmed both configured Stripe test-mode price IDs (`basic`/`pro`) resolve live via a read-only API check. Found and reported, not silently worked around: zero Stripe webhook endpoints were registered on the account and no local forwarding was running, so `stripe listen --forward-to localhost:3000/api/v1/billing/webhook` plus updating `apps/backend/.env`'s `STRIPE_WEBHOOK_SECRET` to the CLI's printed secret was required first; and there is no invite/add-member flow anywhere in the product, so a second test account was moved into the owner's organization as `member` via a direct Supabase SQL edit rather than any in-app flow.

No deviations from the approved plan were observed during manual verification. Phase 3 is now fully closed pending the user's review/commit decision.

## 19. `/settings` — read-only info page (2026-08-08)

Before implementation, a fork surveyed exactly what backend capability exists for a Settings page: every existing auth/organization endpoint is `GET`-only (`GET /auth/me`, `GET /organizations/me`, `/me/usage`, `/me/stats`) — nothing is editable today. Self password/email change, display name (the `profiles` table has no such column), organization rename (the column is schema-updatable but has zero API), and team/member management (listing, invite, role change) all have no backend support whatsoever. Given that, the user picked the smallest workable scope: a strictly read-only page, no new backend or BFF work.

`app/(dashboard)/settings/page.tsx` replaced its `EmptyState` stub with an async Server Component calling `getCurrentUser()` (`services/auth.service.ts`) and `getOrganizationOverview()` (`services/organization.service.ts`) directly via `Promise.all` — the same pattern `DashboardPage` already uses (nothing here is client-interactive, so no BFF route or hook was added). Two `Card` sections, built inline with existing `Card`/`CardHeader`/`CardTitle`/`CardContent` primitives — no new component file, since each section is two static label/value pairs with no branching logic to justify extracting one: **Account** (email, role — `capitalize`d, matching `UserMenu.tsx`'s existing sidebar treatment rather than inventing a new Badge pattern for a single static string) and **Organization** (name, created date via the existing `formatDate` helper). Deliberately not duplicating usage meters (Dashboard's `CurrentPlanCard`) or plan/subscription info (`/billing`).

**Tests**: none added. `services/auth.service.ts`/`services/organization.service.ts` have no dedicated test files and never have — confirmed `dashboard/page.tsx` (the closest existing precedent, same "Server Component fetching via `services/*` and rendering") has none either, so this doesn't introduce a new gap, just follows the existing convention.

**Verification**: full frontend `typecheck`/`lint`/`test` clean — 340 tests (unchanged, as expected). Diff reviewed: exactly one file changed. Manual browser verification for both roles: an `owner` account correctly shows its own email/"Owner"/org name/creation date; a second account, reassigned to the same org as `member` via a direct Supabase SQL edit (no invite flow exists — the same technique already used for Billing Phase 3's manual verification), correctly shows "Member" and the org's real data, not the throwaway org it originally signed up with. No console errors either way. No deviations from the approved scope.
