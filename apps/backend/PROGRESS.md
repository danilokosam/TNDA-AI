# TNDA-AI Backend — Progress

## 1. Summary of Work Completed

A working, type-checked (strict TypeScript, zero `as` casts) Bun + ElysiaJS backend was built from scratch. The server boots, responds on all routes, and every request/response is validated with Zod (used directly as Elysia's Standard-Schema validators — no duplicate TypeBox/Zod schema pairs).

### Modules & endpoints

| Module | Endpoint | Auth | Purpose |
|---|---|---|---|
| health | `GET /health` | none | liveness check |
| auth | `POST /api/v1/auth/signup` | none | creates a Supabase auth user + organization + owner profile in one flow, rolls back the auth user if org/profile creation fails |
| auth | `POST /api/v1/auth/login` | none | Supabase password sign-in, returns session + profile |
| auth | `GET /api/v1/auth/me` | required | current user + organization |
| organization | `GET /api/v1/organizations/me` | required | org record + usage summary |
| organization | `GET /api/v1/organizations/me/usage` | required | pages/documents used vs. plan limits for the current billing period |
| billing | `GET /api/v1/billing/plans` | required | plan catalog (free/basic/pro) |
| billing | `GET /api/v1/billing/subscription` | required | org's effective plan + subscription row (or implicit free tier) |
| billing | `POST /api/v1/billing/checkout` | required | creates a Stripe Checkout Session (`{ planId: 'basic'\|'pro', redirectUrl? }` → `{ url }`) — see §9 |
| billing | `POST /api/v1/billing/portal` | required | creates a Stripe Customer Portal session (`{ returnUrl? }` → `{ url }`) |
| billing | `POST /api/v1/billing/webhook` | **public** — Stripe signature, not bearer auth | receives `checkout.session.completed` / `customer.subscription.created`\|`updated`\|`deleted`, syncs `subscriptions` — see §9 |
| documents | `POST /api/v1/documents` | required | multipart upload, single file **or** `.zip` batch, plus an optional `documentType` field (`invoice`\|`receipt`\|`identity_document`\|`generic`, defaults to `invoice`) that selects the processing strategy — see §6, §13 |
| documents | `GET /api/v1/documents/jobs/:id` | required | polls job status, pulling a fresh status from Azure if still in flight |

Swagger UI is mounted at `/docs`.

### Utilities & cross-cutting concerns

- **`src/utils/file-inspector.ts`** — detects true MIME type from magic bytes (`file-type`, never trusts client `Content-Type`), reads PDF page count via `pdf-lib` without rendering pages.
- **`src/utils/zip.ts`** — in-memory `.zip` extraction (`fflate`, no temp files), filters directories, `__MACOSX/`, `.DS_Store`, empty entries.
- **`src/utils/errors.ts`** — `AppError` hierarchy (`ValidationError`, `UnauthorizedError`, `QuotaExceededError`, `PayloadTooLargeError`, `UnsupportedFileTypeError`, `RateLimitedError`, `AzureServiceError`, …), each carrying an HTTP status + stable machine code.
- **`src/middlewares/error.middleware.ts`** — global `onError` normalizes every thrown error (ours and Elysia's own validation/parse/not-found errors) into `{ error: { code, message, details? } }`.
- **`src/middlewares/auth.middleware.ts`** — verifies the Supabase bearer token, loads the caller's `profiles` row, injects `auth: { userId, email, organizationId, role }` into context for every protected route.
- **`src/middlewares/rate-limit.middleware.ts`** — in-memory fixed-window limiter keyed by client IP (`RATE_LIMIT_WINDOW_MS` / `RATE_LIMIT_MAX_REQUESTS`); swap the `Map` for Redis if the app is ever scaled horizontally.
- **`src/services/azure-document-intelligence.service.ts`** — talks to Azure Document Intelligence over its **REST API directly** (not the `@azure/ai-form-recognizer` SDK — see note below), implements the 202/Operation-Location pattern by hand, and retries `429` responses with exponential backoff + jitter (honors `Retry-After`).

> **Design note on the Azure SDK vs. REST API:** the task allowed either. The SDK's `beginAnalyzeDocument` poller is designed to poll within one continuous process; this app instead persists the `Operation-Location` URL on `document_jobs` and resumes polling from a **separate, later HTTP request** to `GET /jobs/:id` (possibly hitting a different server instance). Driving the submit/poll REST calls by hand fits that architecture more directly and avoids fighting the SDK's poller-serialization model — so `@azure/ai-form-recognizer` was removed from `package.json` in favor of `fetch`.

## 2. Project Architecture Map

```
src/
  config/            env validation (Zod), Supabase client, Azure REST config, hand-written DB types
  modules/
    auth/            signup / login / me
    organization/     org profile, usage & quota computation (owns "effective plan" logic)
    billing/          plan catalog, current subscription, Stripe Checkout/Portal/webhook sync
    documents/        upload (single + zip), pre-flight quota checks, job polling
      *.routes.ts      HTTP layer (Elysia routes, Zod validation)
      *.service.ts     business logic
      *.repository.ts  Supabase access (service-role client; every query filters by organization_id itself since RLS is bypassed)
      *.schema.ts      Zod DTOs
      documents.strategy.ts  document-type -> processing-strategy registry (owns Azure model selection — see §13)
  services/          external integrations — azure-document-intelligence.service.ts
  middlewares/        auth, centralized error handling, rate limiting
  utils/             file inspection, zip extraction, AppError hierarchy
  index.ts           app wiring (cors, swagger, error/rate-limit middleware, route mounting)
supabase/migrations/  numbered SQL migrations (see §3)
```

**Backend-uses-service-role-key pattern:** all repositories query through `supabaseAdmin` (the service-role client), which bypasses RLS, and every query explicitly filters by `organization_id`/caller identity in application code. The RLS policies in the migrations are defense-in-depth for any future direct-from-client Supabase access, not the primary tenant-isolation mechanism for this API.

## 3. Database Migrations Status

**Applied and verified against the live Supabase project** (see §7). Nine SQL files in `supabase/migrations/`, applied in order:

1. `0001_extensions.sql` — `pgcrypto` for `gen_random_uuid()`
2. `0002_organizations.sql` — `organizations` table only (RLS/policies deliberately deferred — see `0004`)
3. `0003_profiles.sql` — `profiles` table (`owner`/`admin`/`member` role enum), FK to `auth.users` (RLS/policies deferred — see `0004`)
4. `0004_authorization.sql` — `current_organization_id()` helper + the RLS policies for `organizations` and `profiles` that call it. Split into its own migration, run after both tables exist, because `current_organization_id()` is a `LANGUAGE SQL` function whose body queries `profiles` — Postgres validates a SQL-language function's body against the catalog at `CREATE FUNCTION` time (unlike `plpgsql`, which defers to runtime), so defining it any earlier fails. See §7 for how this was discovered.
5. `0005_plans.sql` — `plans` catalog, seeded with `free` / `basic` / `pro` rows (idempotent `upsert`)
6. `0006_subscriptions.sql` — `subscriptions`, one active/trialing/past_due row per org enforced via partial unique index
7. `0007_document_jobs.sql` — `document_jobs` table + status enum (`pending/processing/completed/failed/rejected_quota`) + `updated_at` trigger
8. `0008_usage_functions.sql` — `get_organization_monthly_usage(org_id)` SQL function used by the pre-flight quota check
9. `0009_stripe_billing.sql` — adds `organizations.stripe_customer_id` (unique, nullable) and `subscriptions.stripe_customer_id`/`stripe_subscription_id` (the latter a **plain, non-partial** unique index, not the partial-index style used elsewhere in this schema — see §9 for why) for Stripe webhook sync

RLS is enabled on every table with `organization_id`-scoped `select` policies (and a same-org `insert` policy on `document_jobs`); writes beyond that are service-role only.

To (re-)apply against a Supabase project, two options are now available (see §7 for full detail — **do not mix the two on the same database**, they track applied migrations independently):

```bash
# Option A: this project's own migration runner (SUPABASE_DB_URL required in .env)
bun run db:migrate

# Option B: Supabase CLI
supabase link --project-ref <your-project-ref>
supabase db push
```

After applying, regenerate `src/config/database.types.ts` from the live schema if it ever drifts from the hand-written version:
```bash
supabase gen types typescript --project-id <your-project-ref> > src/config/database.types.ts
```
(the `Relationships` arrays this project's hand-written types use were shaped to match Supabase's own generator output, so a regenerated file should be a drop-in replacement.)

## 4. Path Alias Refactor (2026-08-06)

All imports under `src/` were refactored from relative paths (`./`, `../`, `../../`) to absolute `@/*` aliases mapped to `src/*`:

- `tsconfig.json` already declared `"baseUrl": "."` with `"paths": { "@/*": ["src/*"] }` (set up during initial scaffolding) — verified unchanged, no edits needed there.
- Every `import`/`import type` in every `.ts` file under `src/` (config, middlewares, all four modules, services, utils, and `index.ts` itself) now uses `@/...`, including same-directory imports (e.g. `./auth.schema` → `@/modules/auth/auth.schema`) rather than leaving those as relative.
- No `node_modules` package imports (`elysia`, `zod`, `@supabase/supabase-js`, etc.) were touched — only first-party relative imports were converted.
- Verified two ways: `bun run typecheck` (`tsc --noEmit`) is clean, and the server was actually booted with `bun run src/index.ts` against a placeholder `.env` to confirm Bun's runtime resolver (which honors `tsconfig.json` `paths` natively, no extra bundler config needed) resolves the aliases correctly, not just the type checker.
- No `as` casts were introduced — this was a pure import-path rewrite, no logic or types changed.

## 5. `.gitignore` & Backend `README.md` (2026-08-06)

- **`.gitignore`** was rewritten from a minimal 6-line file into a comprehensive, production-grade ignore list covering: environment/secrets (`.env`, `.env.*`, with `!.env.example` explicitly re-included), dependencies/caches (`node_modules/`, `.bun/`, `.bun/cache/`, `.npm/`, `.pnpm-store/`), build outputs and logs (`dist/`, `build/`, `out/`, `*.log`, `npm-debug.log*`, `yarn-debug.log*`, `bun-debug.log*`), OS/editor junk (`.DS_Store`, `Thumbs.db`, `.vscode/*` with `settings.json`/`extensions.json` re-included, `.idea/`, `*.swp`), test coverage output, and root-level temp test fixtures (loose `.pdf`/`.zip`/image files dropped at the repo root during manual testing — scoped to the root only, via `/*.pdf` etc., so it never hides intentionally committed fixtures nested in subfolders).
- **`README.md`** was added at the backend root: title/overview, tech stack rationale table, an ASCII directory-structure map of the layered architecture (`config/ → modules/ → services/ → middlewares/ → utils/`), a step-by-step walkthrough of the full request lifecycle (rate limit → auth → pre-flight quota check → ZIP batch handling → Azure's HTTP 202 submit/poll pattern → client polling), a Getting Started section (`.env` setup, `bun install`, `bun run dev/start/typecheck`, Swagger at `/docs`), and a proprietary/restricted-use license notice as specified.
- Both files describe the backend only — no frontend content was added, since there is none in this repository.

## 6. Pre-flight Validation & Zip Batch Logic (how the quota protection works)

`documents.service.ts`:

1. `submitUpload` checks `file.size` against `MAX_UPLOAD_SIZE_MB` (hard server cap) before reading any bytes, then sniffs the first bytes for a ZIP signature to decide single-file vs. batch.
2. **Single file** (`processSingleDocument`): `inspectDocumentFile` → real MIME type + PDF page count → `assertFileMeetsPlanConstraints` (file size vs. `max_file_size_mb`, page count vs. `max_pages_per_document`, both **hard** per-file caps, no DB row on failure) → monthly quota check (`pagesUsed + pageCount > max_pages_per_month` or `documentsUsed + 1 > max_documents_per_month`) → on quota failure, a `document_jobs` row is written with `status = 'rejected_quota'` for audit purposes and a `422` is returned → on success, a `pending` row is created, then submitted to Azure and flipped to `processing`.
3. **Zip batch** (`processZipBatch`): extracts all entries in memory, then loops with a **running in-memory quota counter** seeded from the DB — this is what stops many small files inside one archive from cumulatively blowing through the monthly quota even though each individually looked fine against the initial DB snapshot. Each file gets one of `processing / rejected / rejected_quota / failed` in the response; the archive is never rejected wholesale for one bad file inside it.
4. **Polling** (`getJobStatus`): terminal jobs (`completed/failed/rejected_quota`) are returned straight from the DB with no Azure call; in-flight jobs get one live `GET` against the stored `Operation-Location`, and the row is only written back to the DB when Azure reports a terminal state (`succeeded`/`failed`), to minimize writes.

## 7. Automated Supabase Migration Tooling (2026-08-06)

Added a real migration runner and validated it end-to-end against the live Supabase project (`.env` now has real Supabase credentials).

- **`scripts/db-migrate.ts`** reads every `.sql` file in `supabase/migrations/` (sorted ascending, numeric-aware), applies each one **inside its own transaction** against Postgres, and tracks what's already been applied in a `public._migrations(filename, applied_at)` table it creates on first run — so re-running the script is safe and idempotent; already-applied files are skipped.
- **Why it doesn't use `@supabase/supabase-js`** (as originally asked): `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` authenticate PostgREST, which only exposes specific tables/views/RPC functions — there is no generic "run arbitrary SQL" endpoint, by design. Nothing in our schema defines an `exec_sql`-style RPC either (and creating one would itself require running SQL first). So the script instead connects directly to Postgres over the wire protocol using the `postgres` package (added as a new dependency) and a new env var, **`SUPABASE_DB_URL`** (documented in `.env.example`, optional — only `db-migrate.ts` reads it via `Bun.env`, it was deliberately **not** added to `src/config/env.ts`'s Zod schema so the main app's boot-time validation and every other script/route stay unaffected by whether it's set).
- **`package.json`** gained two scripts: `db:migrate` (`bun run scripts/db-migrate.ts`) and `db:push` (`supabase db push`, the Supabase CLI's own equivalent, offered as an alternative). **These two are not interoperable** — `db:migrate` tracks state in `public._migrations`, the CLI tracks it in `supabase_migrations.schema_migrations`; mixing them against the same database means neither tool will know what the other already applied. Pick one per environment.
- **Connectivity gotcha hit and resolved during validation:** the direct Postgres hostname (`db.<ref>.supabase.co:5432`) resolves to an IPv6-only address on this Supabase project, and the sandbox this was run from has no IPv6 egress, so the first attempt failed with `ECONNREFUSED`. Fixed by switching `SUPABASE_DB_URL` to Supabase's **connection pooler** (Supavisor) endpoint instead — `aws-<n>-<region>.pooler.supabase.com:5432` with username `postgres.<project-ref>` — which is IPv4-compatible. Worth knowing if this is ever run from another IPv6-less environment (most CI runners, some sandboxes).
- **A real bug was caught and fixed by this first live run**, not by typechecking (which can't catch this — it's a Postgres catalog-validation-order issue, invisible to TypeScript): `0002_organizations.sql` originally defined `current_organization_id()` (a `LANGUAGE SQL` function whose body queries `public.profiles`) before `profiles` existed (`profiles` was created in `0003`). Postgres validates `LANGUAGE SQL` function bodies against the catalog at `CREATE FUNCTION` time — unlike `plpgsql`, which defers to first call — so this failed immediately on the real database with `relation "public.profiles" does not exist`. The transaction wrapper meant this rolled back cleanly with no partial state. **Fix:** split the migrations — `0002` and `0003` now contain only their tables (no RLS/policies), and a new `0004_authorization.sql` defines `current_organization_id()` plus the `organizations`/`profiles` RLS policies that depend on it, now correctly ordered after both tables exist. The old `0004_plans.sql` → `0007_usage_functions.sql` were renumbered up to `0005`–`0008` accordingly (renamed via `git mv` to preserve history — this happened before any commit had successfully applied them to production, so it was the right time to fix the ordering rather than patch around it).
- **Verified:** `bun run db:migrate` applied all 8 migrations successfully against the live project (`7 migration(s) applied, 1 already up to date` on the second run, after the fix); confirmed via a direct query that all six tables (`organizations`, `profiles`, `plans`, `subscriptions`, `document_jobs`, `_migrations`) exist and that the `plans` table seeded correctly (`free`: 1 page/document, 2 pages/month; `basic`; `pro`). `bun run typecheck` remains clean after all of this.

## 8. Azure Document Intelligence End-to-End Test (2026-08-06)

Added `scripts/test-e2e-azure.ts`, a self-contained live pipeline test: it spawns its own instance of the app (on a dedicated port, isolated from any `bun run dev` already running), signs up (or logs in, if the email already exists) a fresh test user via the real HTTP API, generates a mock invoice PDF **in memory with `pdf-lib`** (no fixture file on disk), uploads it through the real `POST /api/v1/documents` multipart endpoint, and polls `GET /api/v1/documents/jobs/:id` every 2 seconds until Azure reports a terminal state — printing the full extracted result payload at the end. Response shapes from the API are validated with Zod (`safeParse`/`parse`), consistent with the rest of the codebase — no `as` casts. Run it with `bun run scripts/test-e2e-azure.ts`.

**This surfaced and led to fixing a real, serious bug** — not something a fresh Azure resource alone would have caught; it required actually exercising the full request path with a real signup:

- **Symptom:** every document upload failed with `500 INTERNAL_ERROR: Cannot coerce the result to a single JSON object` — but only when going through the real running server, never when the same Supabase queries were replicated standalone.
- **Root cause:** `auth.repository.ts`'s `signInWithPassword` (called by both signup and login) was calling `.auth.signInWithPassword()` on the **shared `supabaseAdmin` singleton**. supabase-js mutates a client's own internal session on any successful sign-in, and from then on uses that session's user JWT — instead of the key the client was created with — as the Authorization header for every subsequent `.from()`/`.rpc()` call made on that *same client instance*. Since `supabaseAdmin` is a module-level singleton shared across the entire process, the **first** user to sign up or log in silently downgraded it from `service_role` to their own `authenticated` role for **every other request the server handles afterward**, until another sign-in overwrote it again.
- **Why it looked like a document-upload bug specifically:** the `document_jobs` RLS policies (0004_authorization.sql) grant `authenticated` users `select` and `insert` (scoped to their own org/user), by design, but deliberately **no `update`/`delete`** policy — those are meant to be service-role-only. So under a downgraded client, `INSERT` kept succeeding (matched the insert policy) while every subsequent `UPDATE` — marking a job `processing` or `failed` — silently matched 0 rows under RLS and threw. This is exactly why it reproduced 100% of the time through the real signup→upload flow but never in a standalone script that skipped sign-in.
- **Fix:** `src/config/supabase.ts` gained `createAuthClient()`, which constructs a **fresh, one-off** client (with the anon key) for password sign-in only, never reusing or mutating `supabaseAdmin`. `auth.repository.ts`'s `signInWithPassword` now calls `createAuthClient().auth.signInWithPassword(...)` instead of `supabaseAdmin.auth.signInWithPassword(...)`. `auth.admin.createUser()` and `auth.getUser(token)` (used by `auth.middleware.ts` on every request) were both verified empirically to **not** mutate client session state, so they were left on `supabaseAdmin`.
- **Incidentally, this also answers §7's "RLS is unverified" concern from the previous entry**: chasing this bug down *was* an end-to-end exercise of the RLS policies as a non-service-role client, and they behaved exactly as designed (insert allowed, update correctly denied) — which is what made the bug diagnosable in the first place.
- Also added lightweight `console.error` logging of the full Postgres/PostgREST error object (code, details, hint) in `documents.repository.ts`'s `createDocumentJob`/`updateDocumentJob` and in `documents.service.ts`'s `submitToAzure` catch block — previously these were fully swallowed into a bare `.message` string, which is what made this bug slow to diagnose. Kept permanently, not just for this session, since server-side visibility into DB failures is worth it and these never leak to the client response.

**Verified:** `bun run scripts/test-e2e-azure.ts` completes successfully end-to-end against the real Azure resource — job goes `processing` → `completed` after 4 polls (~8s), and the printed `resultJson` contains genuine Azure `prebuilt-invoice` extraction output (`docType: "invoice"`, confidence `1`, fields like `BillingAddressRecipient` correctly pulled from the mock PDF). `bun run typecheck` remains clean.

## 9. Stripe Billing & Subscriptions (2026-08-06)

Full checkout → webhook → subscription-sync flow, added to the `billing` module (which gained a `billing.repository.ts` and `billing.schema.ts` it didn't have before):

- **`src/config/stripe.ts`** — a single `stripe` client built from `STRIPE_SECRET_KEY`, plus `STRIPE_PRICE_ID_BY_PLAN` (`{ basic, pro }`) mapping our plan slugs to the two Stripe Price IDs from env. `env.ts` gained `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` / `STRIPE_PRICE_ID_BASIC` / `STRIPE_PRICE_ID_PRO` as required fields (`APP_URL` already existed from initial scaffolding and is reused as the default Checkout/Portal redirect base).
- **`POST /api/v1/billing/checkout`** (`{ planId: 'basic'|'pro', redirectUrl? }` → `{ url }`): resolves the organization's Stripe Customer, creating one via `stripe.customers.create()` and persisting it to `organizations.stripe_customer_id` on first use (`resolveOrCreateStripeCustomer`), then creates a `mode: "subscription"` Checkout Session with `client_reference_id`/`metadata` set to the organization id, and returns the hosted Checkout URL.
- **`POST /api/v1/billing/portal`** (`{ returnUrl? }` → `{ url }`): creates a Billing Portal session for the org's existing Stripe Customer; `422`s with a clear message if the org has never checked out (no customer yet).
- **`POST /api/v1/billing/webhook`** (public): registered **before** `.use(authMiddleware)` in `billing.routes.ts` (same pattern as `auth.routes.ts`'s public signup/login), since Stripe authenticates via the `stripe-signature` header, not a bearer token. Uses Elysia's `parse: "text"` route option (plus `body: z.string()` so the type inference matches) to get the **exact raw body string** — required because Stripe's HMAC signature check is byte-sensitive and would break on a JSON-parsed-then-re-serialized body. `verifyStripeWebhookEvent` calls `stripe.webhooks.constructEventAsync(rawBody, signature, STRIPE_WEBHOOK_SECRET)` (the async variant, Stripe's recommended one for non-Node runtimes like Bun, since it uses Web Crypto instead of Node's sync `crypto`) and throws `ValidationError` (400) on a bad/missing signature, per the task's explicit requirement.
- **Event handling** (`handleStripeWebhookEvent`): `customer.subscription.created`/`updated`/`deleted` all funnel through one `syncSubscriptionFromStripeObject` function that reads the org via `organizations.stripe_customer_id`, reads plan + billing period from `subscription.items.data[0]` (**not** top-level `subscription.current_period_start/end` — Stripe moved those fields to the item level in this API version, since a subscription can now have multiple items with independent billing cycles), maps the Stripe price id back to our `plan_id` and Stripe's richer status set (`incomplete_expired`, `unpaid`, `paused`, ...) down to our 5-value `subscription_status` enum, and **upserts by `stripe_subscription_id`** — so `created`→`updated`→`deleted` for the same subscription all converge on one row regardless of delivery order. `checkout.session.completed` retrieves the full subscription object and reuses the same sync function immediately, rather than waiting on a possibly-delayed separate `customer.subscription.created` delivery. Unrecognized customers/price-ids are logged and skipped (return `200`) rather than thrown as errors — retrying a webhook won't fix a config problem, so erroring would just cause Stripe to retry pointlessly for up to 3 days.
- **A real cast-avoidance catch during review:** an early draft of the price-id → plan-id reverse lookup used `Object.entries(...).find(...)` and cast the result back to `PaidPlanId` with `as` — caught before shipping and replaced with a properly-typed `Record<string, PaidPlanId>` reverse map instead, keeping the zero-`as` rule intact.
- **A real correctness catch during review:** the initial migration made `subscriptions.stripe_subscription_id`'s unique index **partial** (`where stripe_subscription_id is not null`), mirroring the style used for `organizations.stripe_customer_id` — but PostgREST's `.upsert(..., { onConflict: "stripe_subscription_id" })` generates a plain `ON CONFLICT (stripe_subscription_id) DO UPDATE` with no `WHERE` clause, which can't target a partial index. Fixed by making that one index non-partial before ever applying it live; Postgres already treats every `NULL` as distinct under a plain unique index, so nothing was lost by dropping the partial predicate.

**Verified live**, end-to-end, against the real Stripe API and the live Supabase project (no Stripe CLI available in this sandbox, so webhook delivery was tested by self-signing events with `stripe.webhooks.generateTestHeaderStringAsync()` using the same `STRIPE_WEBHOOK_SECRET` configured in `.env` — this exercises the exact same verification and sync code path a real Stripe-delivered webhook would, just without needing a publicly reachable endpoint):

1. Signup → `GET /billing/subscription` correctly shows the implicit free tier (no `subscriptions` row yet).
2. `POST /billing/checkout` → real `checkout.stripe.com` URL returned; `organizations.stripe_customer_id` persisted.
3. `POST /billing/portal` → real `billing.stripe.com` URL returned, reusing that same customer.
4. `POST /billing/webhook` with a bad signature → `400 VALIDATION_ERROR`, as required.
5. A self-signed `customer.subscription.updated` event → `200`, and `subscriptions` gets a new row (`plan_id: "basic"`, `status: "active"`, correct period dates, both Stripe ids set); `GET /billing/subscription` immediately reflects it.
6. A self-signed `customer.subscription.deleted` event for the *same* subscription id → `200`, and the **same row** flips to `status: "canceled"` (confirming the upsert-by-`stripe_subscription_id` convergence works); `GET /billing/subscription` correctly falls back to the free plan again, since `canceled` isn't in the "active" set the partial `subscriptions_one_active_per_org` index / `getActiveSubscription` query looks for.

`bun run typecheck` remains clean throughout (zero `as` casts).

## 10. Testing Suite & CI Pipeline (2026-08-06)

Added Vitest (run via Bun), ESLint (no linter existed before this), and a GitHub Actions workflow — 49 tests across 5 files, all passing, zero `as`/`any` in application code.

- **`vitest.config.ts`** resolves the `@/*` alias (Vite doesn't read `tsconfig.json` `paths` on its own, unlike Bun's own runtime) and sets dummy, non-secret env vars via `test.env` — `env.ts` validates eagerly at import time, so without these every test importing anything env-dependent (nearly everything) would throw before running. Real Supabase/Azure/Stripe credentials are never needed to run the suite. `package.json` gained `test` (`vitest run`, used in CI) and `test:watch`.
- **A real portability bug found immediately**: the first test file that imported `@/config/env` (transitively, via `@/config/stripe`) failed with `ReferenceError: Bun is not defined`. Vitest's worker pool — even when the top-level `vitest` command is launched via `bun run` — doesn't run test files under Bun's own global runtime; it's a Node-compatible worker context where `Bun.*` isn't defined. Fixed by switching `env.ts` from `Bun.env` to `process.env` (Bun keeps the two in sync in its own runtime, so the real app is unaffected; `scripts/*.ts` still use `Bun.env` directly since those only ever run under actual `bun run`, never under Vitest).
- **`eslint.config.js`** (flat config, `typescript-eslint` + `@eslint/js` recommended rules) — nothing existed before. `@typescript-eslint/consistent-type-assertions` is set to `never` (bans `as SomeType`, but not `as const` — confirmed the rule already special-cases const assertions) and `no-explicit-any` is `error`, formalizing the zero-cast convention this project has followed by hand since the start. **Running it for the first time immediately caught one real pre-existing violation**: `file-inspector.ts`'s `isSupportedMimeType` used `(SUPPORTED_DOCUMENT_MIME_TYPES as readonly string[]).includes(mime)` to work around a readonly-tuple/`.includes()` typing mismatch. Fixed with `.some((supported) => supported === mime)` instead, which needs no cast. `@typescript-eslint/no-unused-vars` is off, since `tsconfig.json`'s `noUnusedLocals`/`noUnusedParameters` (enforced by `bun run typecheck`) already cover the same ground — avoids two tools reporting the same issue differently.
- **One deliberate, scoped exception**: `**/*.test.ts` files have `no-explicit-any`/`consistent-type-assertions` turned back off. Supabase's `PostgrestQueryBuilder`/`PostgrestFilterBuilder` chain has 6+ generic type parameters per method (`.select().eq().single()`, etc.); faking that shape exactly to mock it in `billing.repository.test.ts` isn't practical or meaningful, and a test double for an external SDK's response shape is a different risk category than a shortcut in application logic. The rule still applies in full to every application file, and to test files' own assertions/fixtures for our own types — only the Supabase mock's plumbing uses `any`.
- **A design improvement that fell out of trying to write good tests**: `billing.service.ts`'s `syncSubscriptionFromStripeObject`/`handleCheckoutSessionCompleted` originally took the full `Stripe.Subscription`/`Stripe.Checkout.Session` SDK types (30+ required fields each). Building a compile-time-valid fixture for either without a cast wasn't practical, so both were narrowed to local interfaces (`StripeSubscriptionSyncData`, `StripeCheckoutSessionSyncData`) containing only the fields actually read. A real `Stripe.Subscription` still satisfies the narrower shape structurally, so call sites needed zero changes — this is a strictly better design (narrower dependency surface on an external SDK) that testability happened to force.
- **Webhook payloads are signed for real, with zero network calls and zero fixture-typing fights**: tests build a *plain* JS object (never typed as `Stripe.Event`), `JSON.stringify` it, and sign it with the real `stripe.webhooks.generateTestHeaderStringAsync()` (pure local HMAC, no API call) using the same `STRIPE_WEBHOOK_SECRET` the test env sets. The result is handed to the real `verifyStripeWebhookEvent`, whose return type — trusted from the SDK, same as any external API response — is what gives the parsed value its `Stripe.Event` typing. This exercises the actual signature-verification code path a real Stripe delivery would hit, without ever needing a compile-time-valid mega-fixture.
- **Test files, by module**: `src/utils/errors.test.ts` (`AppError` hierarchy); `src/modules/billing/billing.service.test.ts` (pure mapping functions, real-signature `verifyStripeWebhookEvent`, `syncSubscriptionFromStripeObject` with a mocked repository, `handleStripeWebhookEvent` end-to-end from a signed payload); `src/modules/billing/billing.repository.test.ts` (mocked `supabaseAdmin`, all 4 exported functions including their error paths); `src/middlewares/error.middleware.test.ts` (a throwaway, never-`.listen()`ed Elysia app exercised via `.handle(request)`, covering `AppError` → status/JSON mapping, Elysia's own validation/not-found errors, and the 500 fallback); `src/modules/billing/billing.routes.test.ts` (the *real* `billingRoutes` mounted behind the *real* `errorMiddleware`, also via `.handle()` — the public webhook route reachable with no auth header, missing/forged signatures rejected with 400, a real signed delivery reaching the repository layer, and the protected routes correctly 401ing without a bearer token).
- **`.github/workflows/ci.yml`**: runs `bun install --frozen-lockfile` → `bun run typecheck` → `bun run lint` → `bun run test` on every push/PR. **One deviation from the literal task wording worth flagging**: it triggers on `master`, not `main` — this repository's actual default branch (confirmed via `git status`) is `master`, and a workflow scoped to `main` would silently never run here. No GitHub Secrets are required for CI to pass, since the dummy env values live in `vitest.config.ts` and `typecheck`/`lint` don't execute any code that reads env at all.

**Verified**: `bun run typecheck`, `bun run lint`, and `bun run test` (49/49 passing) all pass cleanly, run individually and in the exact sequence the CI workflow uses, immediately after a fresh `bun install --frozen-lockfile`.

## 11. Dockerfile & Production Image (2026-08-06)

Added `Dockerfile` and `.dockerignore`.

- **Five-stage build**, all on `oven/bun:${BUN_VERSION}-alpine`: `base` (manifest + lockfile only, maximizes layer-cache hits across builds where dependencies haven't changed) → `install` (full `bun install --frozen-lockfile`, including devDependencies) → `test` (copies the full source, runs `bun run typecheck && lint && test` as a **build-time gate** — the image does not build at all if any of them fail) → `install-prod` (a separate `bun install --frozen-lockfile --production`, built from `base` rather than from `install`/`test`, so devDependencies never enter this layer's `node_modules` in the first place) → `release` (the final image: production-only `node_modules` from `install-prod`, plus `package.json`/`tsconfig.json`/`src` pulled specifically **from the `test` stage** — which is what forces Docker to actually build and pass that stage, not just make it available; a `release` that never referenced `test` would let Docker skip it entirely).
- **A real conflict between two of the task's own requirements, resolved deliberately**: exclude test files via `.dockerignore` *and* run `vitest` as a build-time gate are in direct tension — if `*.test.ts` is excluded from the build context, vitest finds nothing to run and the "gate" silently passes without checking anything. Resolved by **not** excluding test files from the context (they're needed for the gate to mean anything), and instead deleting them from the `test` stage's filesystem immediately after the test run (`find src -name "*.test.ts" -delete`), before `release` copies `src/` from that stage. Net effect matches the requirement's intent — test files never reach the runtime image — without hollowing out the gate. `.dockerignore` itself excludes `.git`, `.github`, `.env*`, `node_modules`, `coverage`, logs, and OS/editor junk, with a comment at the bottom explaining the test-file exception so it isn't mistaken for an oversight later.
- **Non-root user**: `USER bun` in the `release` stage, relying on the official `oven/bun` images shipping a pre-created, unprivileged `bun` user (this project doesn't create one itself). `COPY --chown=bun:bun` is used for everything copied into `release`, rather than relying on default root-owned-but-world-readable permissions.
- **`HEALTHCHECK`** hits the real `/health` endpoint using Bun's built-in `fetch` (`bun -e "fetch(...).then(r=>process.exit(r.ok?0:1))..."`) rather than `curl`/`wget`, since neither is installed in the minimal Alpine image and adding one just for this would work against the "keep the image small" goal.
- Verified the app binds `0.0.0.0` (`ss -tlnp` showed `*:3000` while running locally), not `127.0.0.1` — necessary for it to be reachable through Docker's port mapping at all; a loopback-only bind is a common, easy-to-miss way to ship a container that a healthcheck (and everything else) can never actually reach.
- **This sandbox can't run `docker build`/`docker run` at all** — `docker` the CLI is installed, but this environment's user isn't in the `docker` group and `sudo` needs interactive auth that isn't available here. Every real build/run has to happen on the user's own machine; that's a hard constraint on this whole section, not a one-time caveat.
- **The user ran the real build and it failed** at `[test 4/5] RUN bun run test`, in the three test files that (transitively) import `@/config/env`: `TypeError: undefined is not an object (evaluating 'z.object')` at `env.ts:3:19`. This is a genuinely useful data point despite the failure: it confirms `oven/bun:1-alpine` *is* a real, pullable tag, and that `base`/`install` (pulling the image, `bun install --frozen-lockfile`) both succeeded — the two things flagged as unverified assumptions in the previous revision of this section turned out fine; the actual problem was elsewhere.
  - **Diagnosis**: zod 3.25.76's `package.json` exports a `"@zod/source"` condition (a Vite↔Zod integration) that points at raw `.ts` source (`./src/index.ts`) instead of the compiled `./index.js`/`./index.cjs`, alongside standard `"import"`/`"require"` entries. Which of those Bun's native module resolver picks can depend on the exact Bun *patch* version — and the Dockerfile had `oven/bun:1-alpine`, the rolling tag, which resolves to whatever the latest 1.x release is *at build time*, not the `1.3.14` this whole project has actually been built and tested against locally and in CI. The error message itself (`undefined is not an object (evaluating ...)`) is JavaScriptCore's format, confirming it's Bun's own engine hitting this during module resolution, not a Node-context issue.
  - **Fix, two parts**: (1) `Dockerfile` now takes `ARG BUN_VERSION=1.3.14` (declared before the first `FROM`, used in both the `base` and `release` stages) instead of the rolling `1-alpine` tag, so the image is pinned to the exact same Bun patch version as local development and `.github/workflows/ci.yml`. (2) `vitest.config.ts` gained `test.server.deps.inline: ["zod"]`, forcing zod through Vitest's own Vite-based transform pipeline instead of Bun's native resolver — a deterministic, JS-level resolution path that can't vary by Bun build, which is also exactly what was asked for when this bug was reported. Belt-and-suspenders deliberately: (1) removes the actual variable that (most likely) caused the mismatch, (2) makes the outcome not depend on that variable at all even if the diagnosis is incomplete.
  - **Re-verified locally after the fix**: `bun run typecheck`/`lint`/`test` (49/49) all still pass with the `server.deps.inline` change in place.
  - **Confirmed fixed**: the user re-ran `docker build -t tnda-ai-backend .` and `docker run` after the fix — both succeeded, and the container bound port 3000 correctly. The `BUN_VERSION` pin + `test.server.deps.inline: ["zod"]` combination is the working fix; both changes are staying in place (not just the one that turned out to matter), since the version-pin is good practice regardless and the inline config makes zod resolution deterministic regardless of Bun version going forward.

## 12. README Overhaul (2026-08-06)

`README.md` was rewritten to reflect everything added since it was first written (back when the project was only auth/organization/billing-reads/documents, pre-Stripe): a new **Billing** row in the tech-stack table, plus new **Testing**/**Linting**/**Containerization** rows; the directory-structure map now shows `config/stripe.ts`, `billing.repository.ts`/`billing.schema.ts`/the three `billing.*.test.ts` files, `error.middleware.test.ts`, `errors.test.ts`, `scripts/`, `.github/workflows/ci.yml`, `Dockerfile`, `.dockerignore`, `vitest.config.ts`, and `eslint.config.js` (all real, previously undocumented); the endpoint table gained `POST /checkout`, `POST /portal`, and the public `POST /webhook`; a new "§6 Stripe Billing Flow" was added to the data-flow walkthrough (checkout session creation, then the full webhook verify → dispatch → sync pipeline); and three new top-level sections — **Testing & Code Quality**, **Continuous Integration**, and **Docker** — document the suite/lint approach, the CI workflow, and how to build/run the image, respectively. Getting Started's Prerequisites/Setup were corrected to mention `SUPABASE_DB_URL` (only needed for `bun run db:migrate`, easy to miss since it's absent from the REST-API credential set) and the IPv6 connection-pooler gotcha from §7, and the scripts table now lists `lint`/`test`/`test:watch`/`db:push` alongside what was already there.

Cross-checked against the actual repository (`find src -name "*.ts"`, `cat package.json`, `.env.example`, directory listings) rather than transcribing PROGRESS.md's narrative directly, so the README reflects what's actually on disk right now, not just what was reported as done at the time. While doing this pass, also caught and fixed two claims in §11 that this file itself had left stale since before the user's build confirmation arrived: "not actually build/run-tested" and "the Docker image has never actually been built or run" — both were true when written, both are false now that a real `docker build`/`docker run` has succeeded (§11's last bullet, §13 below).

## 13. Azure Model Selection Decoupled from Global Config — Strategy Pattern (2026-08-06)

Removed `AZURE_DOCUMENT_INTELLIGENCE_MODEL_ID` as a global setting. Previously the entire app used exactly one Azure model (`prebuilt-invoice`, read from that env var) for every upload regardless of what was actually in the file — a receipt, an ID document, anything, all analyzed as if it were an invoice. Model selection is now a per-request runtime decision owned by the domain layer, via a small strategy pattern.

- **`src/services/azure-document-intelligence.service.ts`** is now a pure infrastructure adapter: `beginDocumentAnalysis(bytes, mimeType, modelId)` takes `modelId` as a required parameter instead of reading `azureDocumentIntelligenceConfig.modelId`. It has zero opinion on which model gets used for what — `src/config/azure.ts` no longer has a `modelId` field at all, and `env.ts` no longer declares `AZURE_DOCUMENT_INTELLIGENCE_MODEL_ID`.
- **New `src/modules/documents/documents.strategy.ts`** (domain layer) owns the actual decision: `DocumentType = "invoice" | "receipt" | "identity_document" | "generic"`, a `DocumentProcessingStrategy` interface (`{ documentType, submit(bytes, mimeType) }`), and a `STRATEGIES: Record<DocumentType, DocumentProcessingStrategy>` registry mapping each type to an Azure-prebuilt-model strategy today. Adding a new document type, pointing one at a different Azure model, or swapping a strategy to a different provider entirely (or a multi-step Azure+LLM pipeline) is a change scoped to this one file — `documents.service.ts`, the routes, and the Azure REST adapter itself are all unaffected either way, which was the explicit goal of this refactor.
- **Scope boundary, stated deliberately, not an oversight**: only *submission* is abstracted by the strategy interface. Status polling (`getJobStatus`) and persistence (`document_jobs.azure_operation_id`) still assume Azure's Operation-Location model, since every strategy today is still Azure-backed. A genuinely non-Azure strategy would also need to address how *that* provider's progress gets checked — intentionally left for whenever (if ever) that's actually needed, rather than generalized speculatively now.
- **`documents.schema.ts`**'s `uploadDocumentSchema` gained `documentType: z.enum(DOCUMENT_TYPES).default("invoice")` — a new multipart form field, defaulting to `"invoice"` specifically to preserve the pre-refactor behavior (the old global default was `prebuilt-invoice`) for any existing caller that doesn't send it. For a `.zip` batch, the requested type applies to every file in the archive — there's no per-file type detection, which wasn't asked for and would be a different, larger feature.
- **`processSingleDocument`/`processZipBatch`/`submitUpload` in `documents.service.ts`** all gained a `documentType` parameter, threaded down from the route to a renamed `submitForAnalysis` (was `submitToAzure` — renamed because it no longer has any Azure-specific knowledge itself; it just resolves a strategy via `getProcessingStrategy(documentType)` and calls `.submit()`).
- **A real bug caught by live verification, not by the unit tests**: the first `generic` → model-ID mapping used `"prebuilt-document"`, which turned out to return a genuine `404 ModelNotFound` from Azure's `2024-11-30` API — that model ID has apparently been retired/renamed server-side and the unit tests (which mock the Azure adapter) had no way to catch this, since they only assert *that* the right model ID string gets passed through, not that Azure actually recognizes it. Caught by manually uploading a real file with `documentType: "generic"` against the live server and seeing the real Azure error. Fixed by switching `generic` to `prebuilt-layout` (Azure's stable general OCR/structure model) — arguably a better semantic fit for "generic" anyway, since it makes zero domain assumptions at all, unlike `prebuilt-document`'s key-value heuristics.
- **Also fixed a self-inflicted verification mistake worth recording**: a stray `bun src/index.ts` process from earlier in this session was still bound to port 3000 (from before this refactor's code changes), so the first live verification attempt actually hit stale, pre-refactor code and silently "confirmed" the wrong model was used. `EADDRINUSE` in the new process's own log was the tell. Killed the stale process before trusting any further manual verification.

**Verified**: `bun run typecheck`/`lint`/`test` (53/53, +4 new tests in `documents.strategy.test.ts` covering the registry, per-type model uniqueness, and the `operationLocation` → `operationReference` mapping) all pass. Live-verified against the real Azure resource: all four document types (`invoice`, `receipt`, `identity_document`, `generic`) independently accepted with `202` and the correct `documentModels/<model-id>/...` URL persisted to `document_jobs.azure_operation_id` for each; `scripts/test-e2e-azure.ts` (default `documentType`, full submit → poll → completed cycle) still passes end-to-end; an invalid `documentType` value is correctly rejected with `400 VALIDATION_ERROR` before ever reaching Azure.

## 14. Document List/History + Analytics Endpoints (2026-08-07)

The frontend's Dashboard and History features needed a list/filter/aggregate capability the backend never had (only single-job-by-id lookup existed). Rather than have the frontend read `document_jobs` directly via Supabase RLS to work around that — the originally-planned frontend-side workaround — the backend gained two new endpoints, keeping it the single source of truth for querying, filtering, pagination, and analytics. Designed as stable, general-purpose REST endpoints (predictable envelope shapes, opaque pagination cursors, parameterized filters), not shaped around this specific frontend.

- **`GET /api/v1/documents`** — cursor-paginated, filterable job list. Query params: `status`, `documentType`, `search` (matches `file_name`), `dateFrom`/`dateTo`, `cursor`, `limit` (1-100, default 20). Response: `{ data: JobDto[], pagination: { nextCursor, hasMore } }`. `JobDto` gained `documentType`/`averageConfidence` (previously computed/discarded at upload time, now persisted — see below).
  - **Keyset pagination on `created_at` alone**, not offset (`.range()`) and not a compound `(created_at, id)` cursor. Offset pagination drifts under concurrent inserts — a real case here, since a batch upload can add rows while someone's paging through their own history. A compound cursor would need Supabase's `.or()` raw-filter-string DSL to express `(created_at, id) < (cursor)`, which means interpolating decoded, client-supplied cursor content into a filter *expression* rather than a filter *value* — accepted the simpler, safer trade instead: two jobs created in the exact same instant could theoretically land on opposite sides of a page boundary, rare enough (each row is written by a separate awaited round trip) not to be worth the larger attack surface.
  - The pagination cursor is opaque on the wire (base64url of the encoded `created_at`) — callers pass back exactly what `nextCursor` gave them, never construct one themselves, so the encoding can change later without breaking any client's request shape. A malformed/tampered cursor is treated as "no cursor" (first page), not a request error.
- **`GET /api/v1/organizations/me/stats`** — success rate, average processing time, and a daily job-count series for charting. Query: `since` (ISO datetime, defaults to 30 days back, bounded to 400 days back and not in the future — both enforced by `organization.schema.ts`, not just documented). Backed by two new `SECURITY INVOKER` Postgres functions (`get_organization_job_stats`, `get_organization_daily_job_counts` — migration `0010_document_analytics.sql`), following the existing `get_organization_monthly_usage` precedent for aggregation in SQL rather than pulling rows into the app to sum. Deliberately **not** `security definer`/`grant`ed to `authenticated` like that earlier function is — these are only ever called by the service-role client, which already bypasses RLS regardless, so granting broader access would be unnecessary surface area.
  - `successRate` is `null`, not `0`, when there are no terminal (completed/failed) jobs in the window — `0` would misleadingly read as "100% failure" when the honest answer is "no data yet."
  - The daily-count series is **gap-filled to a complete, contiguous date range** in the service layer (`organization.service.ts#buildDailySeries`) — the Postgres function itself only returns days with actual activity (a plain `GROUP BY`), and a day missing from a sparse result is indistinguishable from "no data at all" to a client that doesn't know the query's internals. Every consumer of this API gets a complete, chart-ready series without needing to know that detail.
- **`document_jobs.document_type` and `.average_confidence` are now actually persisted**, not just computed and discarded. `document_type` was already resolved per-upload (it picks the Azure model) but never saved to the row; now written on every `createDocumentJob` call. `average_confidence` is computed at job-completion time by a new `src/utils/confidence.ts#computeAverageConfidence`, which averages every field-level confidence in Azure's `documents[].fields{name:{confidence}}` shape (the invoice/receipt/identity_document models) and returns `null` — not `0` — for `generic` (`prebuilt-layout`), which has no per-field confidence at all.

**Verified against the real database, not just typechecked**: seeded 15 synthetic `document_jobs` rows (varied statuses/types/dates/confidence, direct SQL insert — real Azure calls aren't relevant to testing list/filter/pagination logic) for a real test org, then confirmed live against the running server: default listing, `limit`-bounded pagination with a working cursor into page 2 (no overlap, no gaps), every filter (`status`, `documentType`, `search`) independently narrowing results correctly, invalid query values rejected with `400 VALIDATION_ERROR` and a clear per-field message, unauthenticated requests rejected with `401`, and the stats endpoint's default/custom `since`, gap-filled `dailyCounts`, and both out-of-range rejections (`since` too old, `since` in the future) all behaving exactly as designed. Synthetic rows deleted afterward.

**Tests**: 34 new tests across 4 new files (`documents.repository.test.ts`, `documents.routes.test.ts`, `organization.repository.test.ts`, `organization.service.test.ts`) — 94 total, up from 60, all passing. `documents.routes.test.ts`/`organization.routes.test.ts` are the first tests in this codebase to exercise an authenticated route's success path rather than only its 401 case — they mock `supabaseAdmin.auth.getUser` and the `profiles` lookup `auth.middleware.ts` itself makes, establishing the pattern for any future route test that needs auth to succeed. `organization.service.test.ts` uses `vi.useFakeTimers()` to test the gap-filling date-series logic deterministically, since it's computed relative to `new Date()`.

**A real dependency-resolution bug found (not by this endpoint work directly, but during the monorepo migration that happened alongside it)**: `apps/backend/package.json` depended on `@types/bun`, and `tsconfig.json`'s `types` points at `bun-types` — a *different* package name `@types/bun` merely depends on internally. Bun's own installer resolved this loosely enough not to notice; pnpm's strict, non-hoisting `node_modules` (adopted in the monorepo migration, see the root `PROGRESS.md`) doesn't, and `tsc --noEmit` failed immediately with `Cannot find type definition file for 'bun-types'` on the first real `pnpm install`. Fixed by depending on `bun-types` directly.

## 15. Wave 1 — ZIP-bomb protection, auth/upload-security test coverage, CI dependency scanning (2026-08-08)

Backend-specific detail for the Wave 1 pass described in full at the root `PROGRESS.md` §3.20 (grounding in the companion audit, cross-app scope, the six-item breakdown) — this section covers only what's specific to this app.

### `utils/zip.ts`: streaming extraction, not `unzipSync`

`extractZipEntries` previously handed the whole archive to fflate's `unzipSync` in one call — no limit on entry count, per-file size, or total decompressed size, and no way to interrupt a bad archive mid-decompression. Rewritten around fflate's incremental `Unzip` class: the archive is pushed in bounded 64KB chunks (not all at once — reading fflate's own `inflt` source confirmed its internal output buffer only grows chunk-by-chunk as input is fed, so this is what actually lets a resource check abort mid-expansion), with `UnzipInflate` registered explicitly (`Unzip`'s default constructor only auto-registers the "stored"/uncompressed decoder, not DEFLATE — confirmed by reading the source, not assumed). Three new exported constants: `MAX_ZIP_ENTRIES` (500), `MAX_ZIP_ENTRY_UNCOMPRESSED_BYTES` (100MB — 2× the `pro` plan's 50MB/file limit, the largest real case), `MAX_ZIP_TOTAL_UNCOMPRESSED_BYTES` (300MB). A declared size is checked upfront where the local file header provides one; the real, running decompressed byte count is independently tracked via the `ondata` callback and enforced as bytes actually stream out, so a header that lies (or, for a streaming-created archive, never declares a size at all) is caught the same way. A cheap End-Of-Central-Directory signature scan up front preserves the "not a valid .zip" rejection for non-zip input, since the streaming reader otherwise has no reason to notice a missing EOCD on its own (it only recognizes local file headers as it scans, unlike `unzipSync`'s central-directory-based parsing).

`zip.test.ts` (new, 10 tests) proves the real production constants, not scaled-down stand-ins — highly-compressible (all-zero) fixtures keep the *compressed* test archives at a few KB and fast to build/run, while genuinely exceeding the real limits once decompressed. One test specifically builds its fixture via fflate's streaming `Zip`/`ZipDeflate` writer (not `zipSync`) so the resulting entry has no declared size at all, proving the incremental byte-counting guard catches a bomb even when the cheap upfront check has nothing to check against.

### Auth module + rate limiter: from zero test files to full coverage

`auth.repository.ts`, `auth.service.ts`, `auth.middleware.ts`, `auth.routes.ts`, `rate-limit.middleware.ts` — 44 new tests. Reused this codebase's established conventions throughout: `vi.mock("@/config/supabase")` plus a thenable query-builder-chain mock (`documents.repository.test.ts`'s pattern) for the repository/routes layers; a minimal `Elysia().use(errorMiddleware).use(authMiddleware)` app dispatched via `.handle()`, no port bound, for the middleware itself. `rate-limit.middleware.test.ts` needed one new pattern: its window state lives in a module-level `Map` (a real, deliberate singleton per the module's own docstring), so tests can't use `vi.resetModules()` between cases the way a stateless module could — resetting modules would also reload `@/utils/errors` fresh, breaking `errorMiddleware`'s `instanceof AppError` check against errors thrown from a separately-reloaded copy of the rate limiter (confirmed by hitting this exact failure once, not anticipated in advance). Fixed by advancing the fake clock past a full window in `beforeEach` instead — the same natural-expiry path the middleware already uses internally, not a test-only escape hatch.

**Bug fixed**: `signUp`'s rollback (`deleteAuthUser` on any failure after the Supabase auth user was created) had no try/catch of its own. If the cleanup call itself failed, it replaced the *original* signup error with an unrelated "failed to delete auth user" one, hiding the real diagnostic. Now caught and logged separately (`console.error`, matching this codebase's existing error-visibility convention); the original error is always what's re-thrown. `auth.service.test.ts` reproduces the exact failure mode.

### Upload-security boundary: `file-inspector.ts` + `submitUpload`

`file-inspector.test.ts` (new, 13 tests) proves classification is by real content, not filename — a file named `invoice.pdf` whose actual bytes are a PNG is correctly reported as `image/png`, the literal security property this module's own docstring already claimed but had never been tested. **Bug fixed, not previously documented anywhere**: constructing a deliberately-corrupt-but-recognizable TIFF fixture (a valid 4-byte signature followed by a bogus IFD offset) made `fileTypeFromBuffer` throw (`End-Of-Stream`, from `strtok3`'s internal tokenizer) instead of returning `undefined` — meaning a real upload shaped like this would have propagated as an unhandled exception instead of a clean `UnsupportedFileTypeError`. Now caught and treated the same as "type not detected." Found empirically while building the test fixture, not predicted — worth remembering if `file-type` or `strtok3` are ever upgraded, in case detection-failure behavior for other formats changes too.

`documents.service.test.ts` gained a `submitUpload` block (4 tests) — the actual upload entrypoint (size-limit check, zip-vs-single routing), which had zero coverage; this file's own top-of-file comment already said so before this session ("quota math, zip batching... remains largely untested"). Deliberately scoped to just that entrypoint, not a full backfill: the size-limit test uses a duck-typed fake `File` (a plain object with `.size`/`.arrayBuffer()`, not a real multi-megabyte buffer) specifically so it can assert `arrayBuffer` was never called — proving the size check happens before any bytes are read — while staying fast regardless of the configured limit's actual value. Quota math (`processZipBatch`'s running-total logic) and zip-batch internals remain untested, unchanged from before this session.

### CI: dependency-vulnerability scanning

One new step in `.github/workflows/ci.yml`: `pnpm audit --audit-level=high`, between Install and Typecheck. Confirmed locally before choosing the threshold: this repo currently has one pre-existing moderate-severity advisory (`file-type`, GHSA-5v7r-6r5c-r473 — an ASF/Windows-Media-parser infinite-loop bug, irrelevant to the five document types this app actually accepts) that `pnpm audit`'s own `moderate` default would fail on immediately and unconditionally. `--audit-level=high` exits 0 today while still failing the build on anything that actually warrants blocking a merge — reasoning documented inline in the workflow file itself, not only here.

### Verification

Full workspace `typecheck`/`lint`/`test` clean. Backend: 239 tests / 22 files (up from 168/15) — 178 after the ZIP work alone, +44 for auth/rate-limit, +13 for `file-inspector.ts`, +4 for `submitUpload`. No new dependency, no schema/migration change anywhere in this pass.

---

## 16. Wave 2 — durable lifecycle event log & retry-state (2026-08-08)

Backend-specific detail for the Wave 2 pass described in full at the root `PROGRESS.md` §3.21 — grounded in `docs/document-domain-architecture.md`'s §4.1 (event log scope) and §3.2/§5 (retry-state, deferred there explicitly until this session). Full reasoning, including the consistency-model decision, in the new `docs/adr/0011-lifecycle-event-log-and-retry-state.md`. Only what's specific to this app is repeated here.

**Two new migrations** (`0014_document_job_events.sql`, `0015_document_job_retry_state.sql`) — the first real schema change since `0013` (§3.18). `document_job_events`: a new Postgres enum (`document_job_event_type`, 9 values — `job_created`/`processing_started`/`processing_completed`/`processing_failed`/`review_confirmed`/`review_rejected`/`review_reset`/`file_removed`/`document_deleted`) and an append-only table (`document_job_id` FK cascade, `actor_user_id` FK `on delete set null` — mirrors `reviewed_by`'s convention exactly, `from_status`/`to_status` as plain text since different event types transition different axes/enums, a bounded `metadata jsonb`, `created_at`), one index, RLS scoped via the same subquery-on-`document_jobs` pattern `document_field_corrections` already uses, no update/delete policy for any role. `document_jobs` gained `retry_count integer not null default 0` and `is_retryable boolean` (CHECK-constrained to only be set when `status = 'failed'`, mirroring `document_jobs_review_requires_completed`).

**`documents.repository.ts`** gained `insertDocumentJobEvent`/`listDocumentJobEvents`, directly mirroring `insertFieldCorrections`/`listFieldCorrections`'s existing shape and test conventions. No route was added for either — no product need to query this yet (the ADR's §11 boundary).

**`documents.service.ts`** gained one new helper, `recordLifecycleEvent` (records an event, catches and logs any failure, never propagates — the same non-fatal tier as `persistOriginalFile`'s Storage write), called at all nine existing state-transition sites: both `createDocumentJob` call sites in `processSingleDocument`/`processZipBatch` (`job_created`, actor = uploader), `submitForAnalysis`'s success and catch paths (`processing_started`/`processing_failed`, actor = `null` — system-caused, even though a human's upload request is what writes the row), `getJobStatus`'s two terminal Azure-status branches (`processing_completed`/`processing_failed`, actor = `null` again, for the same reason — attributing an Azure outcome to whichever user happened to poll at that moment would misrepresent who caused it), `setDocumentReview` (`review_confirmed`/`review_rejected`), `saveFieldCorrections`'s existing unreviewed-reset branch (`review_reset` — this closes the exact gap `document-domain-architecture.md` §4.1 named: a confirm→correct→reconfirm cycle no longer silently loses the first confirmation), `removeDocumentFile` (`file_removed`), and `deleteDocument` (`document_deleted`). Every one of the four idempotent no-op guards already in this code (quota-rejected zip entries, "already unreviewed," "no file to remove," "already deleted") naturally suppresses its corresponding event too — no event fires for an operation that didn't actually change anything, since `recordLifecycleEvent` is only ever called immediately after a real, successful state-changing write, never speculatively before one.

**Retry classification**, added at the same two call sites `is_retryable` needed touching: `submitForAnalysis`'s catch block classifies via a new `isRetryableSubmissionFailure(error)` — retryable unless the caught `AzureServiceError` carries an HTTP status in `[400, 499]` (Azure explicitly rejected the request); `getJobStatus`'s Azure-reported-failure branch is hardcoded terminal (`is_retryable: false`) — Azure fully processed the request and reported the failure itself, a content-level judgment retrying won't change. `retry_count` stays `0` everywhere — nothing in this pass increments it; that's Wave 3's job once a worker actually retries something.

**Tests**: 25 new (4 repository — `insertDocumentJobEvent`/`listDocumentJobEvents`, mirroring the field-corrections test pattern exactly; 21 service-layer, covering event creation/ordering/actor-attribution for all nine transitions, the four no-op-suppresses-the-event cases, the non-fatal-on-insert-failure guarantee, and all four retry-classification branches: submission 5xx→retryable, submission 4xx→terminal, network-error-with-no-status→retryable, Azure-reported-failure→always terminal). One pre-existing test-order fragility was found and fixed along the way (unrelated to Wave 2's own logic): `storageService.deleteDocumentFile` had no default mock resolution in `documents.service.test.ts`'s `beforeEach`, so a rejection set by one `deleteDocument` test silently leaked into whichever test happened to call it next in file order — harmless while `deleteDocument`'s own tests were the file's last block, but surfaced the moment this session's new tests were appended after it. Fixed by giving it an explicit default resolved value in `beforeEach`, matching `uploadDocumentFile`'s existing treatment.

**Verification**: full workspace `typecheck`/`lint`/`test` clean. Backend: 264 tests / 22 files (up from 239/22 — no new test *files*, only new `describe` blocks in the two already-existing `documents.*.test.ts` files). No new dependency. `database.types.ts` updated by hand to match both new migrations (the project's established convention — see §3 above).

**Follow-up, same session — independent review, then the real migration**: on explicit instruction, applying the migration was kept as a separate step from writing/reviewing it. A from-source re-review (not from memory) of both migrations against `0011`-`0013`'s conventions and of every `recordLifecycleEvent` call site found zero defects — nothing changed. The database target was then classified as this project's development Supabase instance (`NODE_ENV=development`, `localhost` `APP_URL`, no deployment pipeline anywhere in the repo, and the same project this whole file's history already treats as "the live Supabase project" for every manual verification step since §7), and `bun run db:migrate` run for real: `2 migration(s) applied, 13 already up to date`. Applied state confirmed by direct query, not just the runner's exit code — `_migrations` rows, `document_job_events`'s 8 columns with correct types/nullability/defaults, the `document_job_event_type` enum's exact 9 values in order, `document_jobs.retry_count`/`.is_retryable`'s types/defaults, `document_jobs_is_retryable_requires_failed`'s real CHECK definition, both FKs on `document_job_events` (cascade on `document_job_id`, set-null on `actor_user_id`), its index, its RLS policy text, and RLS actually being enabled on the table — every one matched the migration files. `typecheck`/`lint`/`test` re-run after: unchanged, still clean (nothing in this suite executes a live query — everything is mocked at the repository boundary).

One real finding from the re-review, recorded as an open Wave 3 decision rather than fixed (full reasoning in the root `PROGRESS.md` §3.21's follow-up note and decision 61): `beginDocumentAnalysis` already retries HTTP `429` internally before ever throwing, so a persistent `429` reaching `isRetryableSubmissionFailure` already represents sustained rate-limiting — arguably the canonical retryable case, not "Azure rejected this request" — yet falls inside the approved `[400,500)` "terminal" range. Zero functional impact today (`is_retryable` is inert); not changed, since the exact rule was reviewed and approved verbatim in the prior turn.

---

## 18. Wave 3 Phase 1 — job-claiming schema and RPC (2026-08-08)

Backend-specific detail for the Wave 3 Phase 1 pass described in full at the root `PROGRESS.md` §3.22 — a full architecture design review happened first (background processing: claiming, crash recovery, retries, idempotency, the Azure lifecycle, polling, queue-vs-database, worker design, concurrency, the state model, event-log integration, failure modes, tenancy, observability, testing, deployment), approved in principle, with implementation explicitly scoped to Phase 1 only. Only what's specific to this app is repeated here.

**Azure idempotency investigation** (required before any code, per explicit instruction): fetched Microsoft's own REST API reference for API version `2024-11-30` (the exact version `config/azure.ts` targets) and confirmed no idempotency-key/dedup mechanism exists on the `:analyze` endpoint — every submission creates a fresh, server-generated operation unconditionally, with no way to discover an orphaned one afterward. The narrow "submitted to Azure, crashed before persisting the operation id" gap identified by the design review cannot be closed on Azure's side; stays an accepted, documented limitation (ADR 0012). Directly ruled out adding `submission_attempted_at`: its only value would be forensic, it changes no behavior, and this phase doesn't touch the submission code path at all.

**Schema** (`0016_document_job_claiming.sql`, the first migration since Wave 2's `0014`/`0015`): `document_jobs` gains `claimed_by`/`claimed_at`/`lease_expires_at` (the claim), `lease_epoch` (a fencing token, bumped only on a fresh claim, never a renewal), and `next_attempt_at` (backoff scheduling — the column Wave 2's ADR explicitly deferred to this wave). Two CHECK constraints (claim-field all-or-nothing consistency; `next_attempt_at` only meaningful on a retryable failure) mirror this schema's existing "app logic primary, DB constraint backs it up" pattern. One partial index (`document_jobs_claimable_idx`) covers the common pending/processing claim path. Claim state lives directly on `document_jobs`, not a new table — same orthogonal-attribute precedent as `deleted_at`/`review_status`/`retry_count`. No `document_job_events` change — claiming/leasing are worker-operational mechanics (the architecture doc's own kind-(2) taxonomy), not domain events.

**The RPC**, `claim_or_renew_document_job(p_worker_id, p_lease_seconds, p_max_retries)`: renews an existing claim the caller already holds, or atomically claims one eligible row via `SELECT ... FOR UPDATE SKIP LOCKED`, folding a failed-and-retryable row's retry transition (status back to `pending`, `retry_count` incremented, `is_retryable`/`next_attempt_at` reset) into the same statement. `documents.repository.ts` gained `claimOrRenewDocumentJob`, wrapping it exactly the way `organization.repository.ts`'s existing RPC wrappers do.

**Two real defects caught during this phase's own independent review, before the migration was applied**:
1. **A concurrency-slot naming trap.** The design implicitly assumed one `claimed_by` value per worker *process*; tracing through `WORKER_CONCURRENCY > 1` (an already-approved later-phase default) showed a worker holding several jobs at once would have every renewal call silently renew *all* of them instead of claiming new work, since the RPC's renew-vs-claim decision is purely "does a row already have `claimed_by` = this value." Fixed by documenting explicitly, everywhere the parameter appears (migration column comment, RPC comment, repository TSDoc), that it identifies one claim *slot*, not a process.
2. **A real, confirmed security gap.** The migration's first draft assumed omitting a `GRANT ... TO authenticated` was sufficient to keep the RPC service-role-only (mirroring how `document_jobs`' own UPDATE/DELETE already have no `authenticated` policy). Querying `information_schema.routine_privileges` against the real database mid-review proved this wrong twice over: PostgreSQL grants `EXECUTE` to `PUBLIC` by default on function creation, and this specific Supabase project *also* grants `EXECUTE` directly to `authenticated`/`anon`/`service_role` on every new `public`-schema function (a project-level default-privilege rule, not anything this repo's own migrations set up) — confirmed empirically, not assumed from Postgres defaults alone. Fixed with two explicit `REVOKE` statements (`PUBLIC`; then `authenticated`/`anon` by name); `service_role`'s own access was verified, empirically, to survive both.

**Tests**: 3 new unit tests (`documents.repository.test.ts`) covering `claimOrRenewDocumentJob`'s call shape, the null-on-nothing-available case, and the AppError path — mirroring `organization.repository.test.ts`'s existing RPC-mocking convention. Per explicit instruction, the real concurrency/locking/timing behaviors were **not** added as permanent CI-running integration tests (this project's CI stays unit-tests-only for now — a stated, deliberate limitation, not an oversight; revisit only if the worker architecture later makes it necessary). Instead verified via a real-database script run directly against the development Supabase project: two concurrent claims never both win; a pending job is claimable; a retryable failed job becomes claimable only once `next_attempt_at` passes, with the retry transition correct; a terminal failed job is never claimed; an active lease can't be stolen; an expired lease is reclaimable; renewal is owner-only; a stale (reclaimed-over) worker can neither renew nor have an epoch-gated write succeed; organization isolation holds; a soft-deleted job is never claimed; both CHECK constraints reject what they should; the grants themselves are correct (`authenticated`/`anon` denied, `service_role` allowed) — 36 checks, all passing.

**A real, populated dev database needed real care, not just a clean-room assumption.** This database has months of real leftover `pending`/`processing` rows from prior manual verification sessions (already documented in this file's own history — e.g. accumulated `e2e-test-*` rows). The verification script's first two iterations had real flaws surfaced by this: fixture rows lost the claim query's `ORDER BY created_at ASC` race to older real rows (fixed by pinning fixture timestamps far in the past, non-destructively — nothing pre-existing was ever read, claimed, or modified by this fix); and several assertions incorrectly expected a losing concurrent caller to get `null`, when in a live, uncontrolled-population database it legitimately claims some *other* real available job instead (fixed by asserting "didn't get *my* specific job," not "got nothing"). A handful of real historical rows were incidentally claimed by the script's early, still-buggy iterations before these fixes landed — all identified and fully restored to their untouched claim state (verified by a final query showing zero rows with any claim set anywhere in the table) before the phase was considered done.

**Verification**: `pnpm typecheck`/`lint`/`test` clean — 267 backend tests (up from 264), 22 files. `bun run db:migrate` applied `0016` cleanly; every new schema object (both new columns' types/defaults, both CHECK constraints' real definitions, the index, the RPC's grants) confirmed by direct query. No worker, no Azure submission/polling relocation, no HTTP-behavior change anywhere in the diff. Left uncommitted, per explicit instruction, pending review before Phase 2.

---

## 19. Billing — Phase 0: backend test backfill (2026-08-08)

A full analysis of Billing's existing state (backend, frontend, data model, security, TDD coverage) preceded any implementation — not reproduced here, see the root `PROGRESS.md` §3.24. Finding: the backend's Stripe integration (checkout, portal, webhooks, subscription sync) was already fully implemented and had been live-verified once (§9), but two real gaps existed: `createCheckoutSession`/`createPortalSession` — the two functions that actually call the Stripe API — had zero unit tests, and `organization.service.ts`'s plan/subscription functions (`getEffectivePlan`, `getUsageSummary`, `getAllPlans`) had none either, despite already being used by the Dashboard's current-plan card.

Phase 0 closed that gap before any new frontend work started on top of it, per this project's TDD discipline — pure test addition, zero behavior change:

- **`billing.service.test.ts`** (+9): `createCheckoutSession` (existing-customer reuse, new-customer creation + persistence, correct session params, `redirectUrl` fallback to `env.APP_URL`, no-url error) and `createPortalSession` (no-billing-history guard, session creation, `returnUrl` fallback). Stripe's SDK methods are stubbed via `vi.spyOn` on the real `stripe` client's nested methods, not a blanket module mock — the existing webhook tests rely on the real SDK's local HMAC signing and needed to keep working unmodified.
- **`billing.routes.test.ts`** (+5): `GET /plans`, `GET /subscription`, a successful `POST /checkout`, and `POST /portal` (success + the 422 case) — none of these had route-level coverage before; only the webhook and a bare checkout-401 did. Needed adding a `supabaseAdmin`/auth mock this file didn't have yet, mirroring `organization.routes.test.ts`'s established `mockAuthenticated()` pattern exactly. The service layer is mocked for these (not the repository), matching how `organization.routes.test.ts` already tests its own routes — webhook tests are untouched and still exercise the real service.
- **`organization.repository.test.ts`** (+8) / **`organization.service.test.ts`** (+7): `getActiveSubscription`/`getPlanById`/`listPlans` and `getEffectivePlan`/`getUsageSummary`/`getAllPlans` respectively — including the free-tier fallback, the usage-exceeds-limit clamp-at-zero case, and the calendar-month `periodStart` fallback when there's no active subscription.

**Verification**: full workspace `typecheck`/`lint`/`test` clean — 296 backend tests (up from 267), 22 files. No application code touched — confirmed by `git status` showing only the four test files.

## 20. Billing — authorization fix: `owner`/`admin`-only checkout/portal (2026-08-08)

A real, pre-existing gap, found while planning Billing Phase 3 (the frontend UI), not introduced by any of this work: `POST /checkout` and `POST /portal` had never actually enforced the owner/admin restriction locked in as a decision before Phase 0 even started — `auth.role` was available (populated by `authMiddleware` like every other route) but never checked. Any authenticated org member could create a checkout session or open the Stripe Customer Portal for the whole organization. Caught deliberately by checking the real route source before building UI on top of the assumption it was already enforced — exactly the kind of thing that's invisible from the frontend and easy to silently inherit.

Fixed with the smallest possible change, reusing the exact `ForbiddenError`/403 pattern `documents.service.ts#assertCanManageFile` already established for destructive document actions — no new authorization mechanism. `billing.service.ts` gained `assertCanManageBilling(role)`, called at the top of `createCheckoutSession`/`createPortalSession`; both functions' param interfaces gained a required `role: ProfileRole` field; `billing.routes.ts` passes `auth.role` through, exactly as `documents.routes.ts` already does for `removeDocumentFile`/`deleteDocument`. `GET /plans`/`GET /subscription` are deliberately untouched — viewing billing state stays open to any org member, only the money-moving/subscription-managing actions are gated.

**Tests** (TDD, written first): `billing.service.test.ts` (+4) — a plain `member` gets `ForbiddenError` from each function before the repository or Stripe is ever touched; `admin` succeeds identically to `owner`. `billing.routes.test.ts` (+2, plus the two existing "authenticated" tests updated to assert `role` is now part of the service call) — a `member`'s request surfaces the service's `ForbiddenError` as a real `403` through the actual route, and the route is confirmed to pass the caller's true role through rather than deciding anything itself. One incidental fix needed along the way: `billing.routes.test.ts`'s `TEST_PROFILE` had `role: "owner" as const`, which TypeScript narrowed tightly enough to reject passing `role: "member"` to the same `mockAuthenticated()` helper — removed the `as const`, matching `documents.routes.test.ts`'s own (unnarrowed) convention for the identical fixture.

**Verification**: full workspace `typecheck`/`lint`/`test` clean — 302 backend tests (up from 296), 22 files. Diff reviewed and confirmed scoped to exactly `billing.routes.ts`/`billing.service.ts` and their two test files — no Stripe behavior change, no schema/migration, no RLS change, no `documents`/`worker` touch.

---

## 21. Current State & Next Steps

**Verified so far:** `bun install`, `bun run typecheck` (clean, zero errors, zero `as` casts), the server boots and responds correctly (`bun run dev` / `bun run start`) — manually smoke-tested `/health`, `/docs`, auth-guard rejection (401 with structured JSON), Zod validation errors (400 with per-field issues), 404 handling, and multipart auth-gating — **all 16 database migrations are applied and verified against the live Supabase project (§7, §14, §16, §18 — `0014`-`0016` each applied and schema-verified in a dedicated follow-up step, not just trusted from the migration runner's exit code)** — **the full document pipeline (signup → upload → Azure submit → poll → completed, with real extracted fields) is verified working end-to-end against live Azure and Supabase resources (§8)** — **Stripe Checkout, Billing Portal, and webhook subscription sync are verified working end-to-end against the live Stripe API (§9)** — **`bun run typecheck`/`lint`/`test` (302 tests, up from 94 — §15, §16, §18, §19, §20) all pass cleanly and are enforced in CI, which now also runs a dependency-vulnerability scan (§10, §15)** — **Billing's previously-untested `createCheckoutSession`/`createPortalSession` and `organization.service.ts`'s plan/subscription logic now have full unit coverage, and `POST /checkout`/`POST /portal` correctly enforce an owner/admin-only restriction that had never actually been checked before (§19, §20)** — **`docker build` + `docker run` both succeed against the real `Dockerfile`, with the container binding port 3000 correctly (§11)**, after fixing the Bun-version/zod-resolution bug that first build attempt surfaced — **Azure model selection is now a per-`documentType` domain decision instead of one global env var, live-verified for all four document types against the real Azure resource (§13)** — and **the document list/filter/pagination endpoint and the organization stats/analytics endpoint are both live-verified against the real database with seeded data covering every filter, pagination, and validation edge case (§14)**. As of this pass, the backend now runs from `apps/backend/` inside a pnpm + Turborepo monorepo (see the root `PROGRESS.md`) — this file continues to track backend-specific work only.

**Not yet done / explicitly out of scope for this pass:**

- **Test coverage is broader now but still not comprehensive** — §10/§13/§14 cover `billing` (service, repository, routes), the error middleware, `errors.ts`, the document-processing strategy registry, the new list/pagination and stats/analytics endpoints, and `confidence.ts`. §15 (Wave 1) closed the auth/upload-security gaps this bullet used to name: `auth.service.ts`/`auth.repository.ts`/`auth.routes.ts`/`auth.middleware.ts`, `rate-limit.middleware.ts`, `zip.ts`, and `file-inspector.ts` are all now tested, plus `submitUpload`'s size-limit and zip-routing logic specifically. §19 closed the remaining billing gap: `createCheckoutSession`/`createPortalSession` and `organization.service.ts`'s effective-plan resolution (`getEffectivePlan`/`getUsageSummary`/`getAllPlans`) are now tested too. The sign-in path is confirmed to use a fresh `createAuthClient()`, not the shared `supabaseAdmin` singleton — but this is narrower than a full regression guarantee that *no* code path anywhere ever calls a session-mutating method on `supabaseAdmin`; that broader guard still doesn't exist. **Still untested**: `processZipBatch`'s in-memory running-quota math specifically (as opposed to the entrypoint routing into it, which §15 now covers).
- **Only document *submission* is provider-agnostic, not status polling or persistence** — see §13's scope-boundary note. `getJobStatus` and the `document_jobs.azure_operation_id` column still assume Azure's Operation-Location model. A genuinely non-Azure `DocumentProcessingStrategy` (as opposed to a different Azure model) would need that addressed too — deliberately not generalized speculatively in this pass.
- **No per-file document type in `.zip` batches** — a batch upload's single `documentType` field applies to every file in the archive; there's no way to say "these 3 are invoices, these 2 are receipts" in one request today.
- **No background job worker** — `GET /jobs/:id` polls Azure synchronously, on-demand, inside the request handler; `POST /documents` still submits to Azure synchronously too. Wave 3 Phase 1 (§18) added the job-claiming/leasing schema and RPC this would need, and verified it works correctly against the real database — but no worker process exists yet, and no HTTP behavior has changed. Phase 2+ (moving submission/polling into an actual worker) is explicitly not authorized until reviewed.
- **The lifecycle event log (§16) can under-record on a crash/network partition** — a real transition can end up with no `document_job_events` row if the process dies between the state write and the event write. Cannot over-record (a false event) under the write-order model used — see `docs/adr/0011-lifecycle-event-log-and-retry-state.md`. Accepted, not a bug to fix, per the ADR's own reasoning.
- **Team management is not implemented** — `profiles.role` (`owner/admin/member`) exists in the schema and RLS, but there's no invite-teammate / change-role endpoint yet.
- **Rate limiting is single-instance/in-memory** — fine for one server process; needs a shared store (Redis) before horizontal scaling.
- **No real Stripe webhook endpoint registered, and no plan-downgrade enforcement** — §9's webhook sync is verified with self-signed test events (no Stripe CLI in this sandbox), not a live-registered endpoint receiving real Stripe traffic yet; that's the next thing to wire up once there's a public URL. Also, nothing currently re-checks an org's *existing* `document_jobs`/usage against a *new, lower* plan after a downgrade or cancellation — quota is only ever checked at upload time against whatever the current plan is.
- **No idempotency/replay protection beyond Stripe's own signature+timestamp check** — `handleStripeWebhookEvent` doesn't record processed `event.id`s, so if Stripe redelivers the same event (which it does on retries, and can occasionally do even after a `200`), it gets reprocessed. Harmless here since the sync is a pure upsert-by-`stripe_subscription_id` (reprocessing just re-writes the same values), but worth knowing if a future event handler ever does something non-idempotent (e.g. sending an email per event).
- **`scripts/test-e2e-azure.ts` leaves test data behind** — each run creates a new timestamped test user/org (no cleanup step), so the live Supabase project will accumulate `e2e-test-*` users/orgs and their document_jobs over repeated runs. Harmless for a dev/staging project (each run gets a fresh free-tier org, well under quota) but worth periodically clearing out, or extending the script with a teardown step, before this project has real customer data alongside it.
- **No image published anywhere, and `ci.yml` doesn't build/push it** — the CI workflow runs typecheck/lint/test only; it doesn't build the Docker image (so a Dockerfile-breaking change wouldn't be caught by CI today, only by whoever next runs `docker build` locally) or push to any registry. Now that §11's build is confirmed working end-to-end, this is the natural next step.
- **The container has only been smoke-tested (boots, binds the port)** — `docker run` was confirmed to start correctly and bind `3000`, but nothing has yet exercised an actual request (e.g. `curl` against `/health` through the container, or a full signup→upload flow with real credentials passed via `--env-file`) through the running container specifically, as opposed to through `bun run dev`/`start` directly.

**To resume work**, the next most valuable steps in order would be: (1) add an image-build (and ideally a smoke-test request against `/health`) step to `ci.yml` so a broken Dockerfile fails CI, not just a local build, (2) register a real Stripe webhook endpoint (via `stripe listen` locally or a real deployed URL) and confirm live-delivered events sync the same way the self-signed test events did in §9, (3) extend test coverage to `documents.service.ts`'s `processZipBatch` quota math — the auth/upload-security portion of this item is now done (§15), and so is `organization.service.ts`'s plan/subscription logic (§19); this is what's left of it, (4) Wave 3 Phase 2+ — the actual worker process, moving Azure submission/polling into it — the claiming schema/RPC it needs already exists and is verified (§18), but this is explicitly not authorized until reviewed, (5) build the teardown/cleanup step for the E2E test script, (6) if this repo's default branch is ever renamed to `main` (or a `main` branch is intentionally introduced), update `.github/workflows/ci.yml`'s trigger branches to match — right now it's `master` for real triggering, per §10, (7) if/when a genuinely non-Azure `DocumentProcessingStrategy` is actually needed, revisit §13's scope boundary (status polling + `document_jobs.azure_operation_id` are still Azure-specific) rather than generalizing it speculatively now.
